# bl10 T-SPACE — one blank line above every top-level block (plan v2)

> **For agentic workers:** REQUIRED SUB-SKILL: doperpowers:subagent-driven-development, task-by-task.
> Spec: `docs/superpowers/specs/2026-08-31-bl10-menus-click-spacing-design.md` (§2.2, D6–D8, D11, D14,
> D16, A6–A7 — spec v2 governs; the codex plan review's F1/F2/F8 are folded in).
> Research (exact fix sites + canon cites — READ §1/§4 BEFORE Task 1):
> `.doperpowers/sdd/2026-08-31-bl10-round/research-spacing.md` (R3).

**Goal:** implement canon's spacing invariant — exactly one blank row above every top-level RENDERED
block, live-streaming included, plus spinner/composer margins with remeasured dock budgets.

**Global constraints:** commands from `harness/`; `npm run typecheck` + scoped `npx vitest run` per
task; never bare `npm test`. TDD. Snapshot churn is expected and large — every snapshot change must be
ADJUDICATED against R3 §1.5's canon table (the reviewer will spot-check), never blanket-accepted.
Separator = a `kind:"line"` item, empty text, emitted ABOVE the realized unit (spec D6/D11); each
separator id is unique AND durable (pairwise-distinct — `publishedIds` dedup must never be able to
swallow two separators silently); no `marginTop` field on RenderItem/RenderLine. Separators carry NO
`clickable`, NO band/bg, and no foldAnchor. No document-start suppression (spec D7). Commit per task,
no Co-Authored-By. Do not touch `src/appserver/`.

### Task 1 — Separator invariant over REALIZED units + the cluster-member [BUG]

**Files:** modify `src/tui/toolRenderer.tsx` at the concat sites — `weaveStandaloneHooksFlat` fast
path (:1582) and interleave loop (:1587), `foldAnchored`'s out-loop arms (:1747-1763),
`projectPending`'s items loop (:1845-1857) — plus `projectMessageEntry`'s per-content-block loop
(:900-903) and the `expandedMemberItems` member arm (:1097, matching `thinkingRowItems` :1069; canon
`LC` unconditional marginTop:1 at cli.pretty.js:193259). ONE helper (e.g.
`separatorItem(boundaryId)`) so the invariant has one definition.

**CRITICAL (spec D11, plan-review F1):** raw `Anchored` entries are NOT visible blocks.
`projectMessageEntry` filters `tool_use`/`tool_result` content (:846-881) but `buildAnchoredEntries`
retains those empty carriers (:1423-1441), and the visible completed-tool anchor is added separately
(:1605-1611). The separator gates on the unit ACTUALLY CONTRIBUTING items at the concat point —
prepend only ahead of a non-empty push, tracking whether anything rendered has been emitted before it
(the very first rendered unit still gets its separator; canon does not suppress at start).

**Test:** new `test/tui/spacing-invariant.test.tsx`, projection level (`projectCompact` AND
`projectDetail` over a real `TranscriptDocument`): exactly one blank above every rendered anchor;
ZERO separators for empty carriers (a message whose only content was the filtered tool_use, absorbed
thinking under compact, suppressed/coalesced entries) — no phantom or double gaps; zero between tool
header and its `⎿` body; one between expanded-cluster members; the banner seam composes to 2 (banner
trailing blank + leading separator); markdown paragraph gap stays exactly 1 (no stacking with the
new separator); hook rows keep 0-gap absorption (:1103-1116); pairwise separator-id uniqueness
across the whole projection; id durability across re-projection; and a Static commit → reprojection
transition (grow the doc, re-project, assert already-published regions' separators unchanged).
Resolve the `shouldShowDot` consecutive-text-blocks interaction from canon (one message per content
block, each with its own margin ⇒ one separator each); record in the spec Decision Log if the
resolution deviates.

### Task 2 — Live streaming joins the invariant (spec D14)

**Files:** the shared streaming representation (`src/tui/streamingItems.ts`; consumed by
`Transcript.tsx:27-32` classic and `FullscreenViewport.tsx:371` fullscreen — find the single point
both paths share; if none exists, add the separator in `streamingItems.ts` and mirror minimally in
the classic path). **Tests:** first streaming delta already shows one gap from its predecessor; the
gap does NOT accumulate across subsequent deltas; the streaming→finalized transition keeps exactly
one gap (assert the frames around finalization — no geometry jump); an interrupted/aborted stream
leaves no orphan separator. The streaming separator is stable-id'd and unbanded like all others.

### Task 3 — Chrome + dock budgets (spec D16)

**Files:** `src/tui/ChatApp.tsx` (:1985-1987 — wrap the spinner SLOT, shared by
`TurnSpinner`/`RetryRow`/`CompactionRow`, in `<Box marginTop={1}>`; canon `Gn` cli.pretty.js:77727,
unconditional); composer margin — the authoritative suggestion-palette state lives in
`src/tui/ChatComposer.tsx`, wire it to wherever the margin is applied (`composerFrame.tsx:131` is the
mount; canon L160599: 1 normally, 0 while the palette is open); **`src/tui/liveWindow.ts:38-56`** —
`MAIN_DOCK_ROWS` is a documented measured constant (14 = 8 todo + 1 live-turn slot + 1 queue + 3
composer + 1 footer): re-measure with the two new rows → 16, and UPDATE ITS DOC COMMENT itemization;
re-derive `dockDialogRows` (grep for it; it charges the live-turn slot as one row via
`ChatApp.tsx:~1730-1737`). **Tests:** spinner visible ⇒ one blank above it (retry + compaction rows
too — the margin is the slot's, not the verb's); palette open ⇒ composer gap 0, closed ⇒ 1;
tall-write tests at short (e.g. rows=18) and normal heights for spinner/retry/compaction/palette-open
states asserting no Ink `outputHeight >= rows` full clear/replay on ticks (the two-row slack this
change spends).

### Task 4 — Rebaseline + row budgets + battery

Re-run and adjudicate every moved frame test (`f1-frame-parity`, `f3–f6-acceptance`, `render.test.ts`,
`toolRenderer.test.tsx`, `transcriptPager`, `live-window-*` — ROW BUDGETS RE-DERIVED against the
+1-row-per-block geometry with the arithmetic in the task report, not just re-recorded —
`fullscreen-*`, resume/attach replay). Then: `npm run typecheck`; full `npx vitest run test/tui/` +
`test/unit/`; `npm run test:resize-matrix` (tmux; private socket only). Frame-parity for A6: decode
`test/fixtures/upstream-frames/f1-tool-rendering/01-read-complete.ansi` rows 12-24 and assert our
equivalent sequence produces the same blank-row pattern (prompt/gap/tool/body/gap/spinner/gap/rule).
