# bl10 T-SPACE — one blank line above every top-level block

> **For agentic workers:** REQUIRED SUB-SKILL: doperpowers:subagent-driven-development, task-by-task.
> Spec: `docs/superpowers/specs/2026-08-31-bl10-menus-click-spacing-design.md` (§2.2, D6–D8, A6–A7).
> Research (exact fix sites + canon cites — READ §1/§4 BEFORE Task 1): 
> `.doperpowers/sdd/2026-08-31-bl10-round/research-spacing.md` (R3).

**Goal:** implement canon's spacing invariant — exactly one blank row above every top-level transcript
block, plus spinner/composer margins — via the established blank-line stand-in device, then re-verify
all row-budget arithmetic.

**Global constraints:** commands from `harness/`; `npm run typecheck` + scoped `npx vitest run` per
task; never bare `npm test`. TDD. Snapshot churn is expected and large — every snapshot change must be
ADJUDICATED against R3 §1.5's canon table (the reviewer will spot-check), never blanket-accepted.
Separator = a `kind:"line"` item, empty text, stable id, emitted ABOVE the anchor (spec D6); no
`marginTop` field on RenderItem/RenderLine. No document-start suppression (spec D7 note). Commit per
task, no Co-Authored-By. Do not touch `src/appserver/`.

### Task 1 — The separator invariant + the cluster-member [BUG]

**Files:** modify `src/tui/toolRenderer.tsx` at the four concat sites — `weaveStandaloneHooksFlat`
fast path (:1582) and interleave loop (:1587), `foldAnchored`'s out-loop arms (:1747-1763),
`projectPending`'s items loop (:1845-1857) — plus `projectMessageEntry`'s per-content-block loop
(:900-903) and the `expandedMemberItems` member arm (:1097, matching `thinkingRowItems` :1069; canon
`LC` unconditional marginTop:1 at cli.pretty.js:193259). Prefer ONE helper (e.g. `separatorItem(id)`)
so the invariant has one definition. Test: new `test/tui/spacing-invariant.test.tsx` at the
projection level: build a `TranscriptDocument` with prompt/text/Read+result/text/Bash+result/notice,
run `projectCompact` AND `projectDetail`, assert exactly one blank item above every anchor, zero
between a tool header and its `⎿` body, one between expanded-cluster members, the existing banner
trailing blank + new leading blank composing the 2-blank banner seam, and stable separator ids across
re-projection (Static-region safety).

Watchpoints the tests must pin: (a) no DOUBLE blank where a producer already emits a leading blank
(`thinkingRowItems` :1069 keeps exactly one; markdown paragraph gap stays exactly 1); (b) the
`shouldShowDot` consecutive-text-blocks rule vs. the :900 seam — decide from canon: canon emits one
message per content block, each with its own margin, so consecutive retained content blocks get one
separator each; record the resolution in the spec's Decision Log if it deviates; (c) hook rows keep
their 0-gap absorption (:1103-1116).

### Task 2 — Chrome: spinner slot + composer

**Files:** modify `src/tui/ChatApp.tsx` (:1985-1987 — wrap the spinner SLOT, shared by
`TurnSpinner`/`RetryRow`/`CompactionRow`, in `<Box marginTop={1}>`; canon `Gn` cli.pretty.js:77727
unconditional), `src/tui/composerFrame.tsx` (:131 — `marginTop` 1, dropped to 0 while the suggestion
palette is open; canon L160599). Tests: extend the ChatApp mount/frame tests: spinner visible → one
blank above it; palette open → composer gap 0; palette closed → 1.

### Task 3 — Rebaseline + row budgets + battery

Re-run and adjudicate every moved frame test (`f1-frame-parity`, `f3–f6-acceptance`, `render.test.ts`,
`toolRenderer.test.tsx`, `transcriptPager`, `live-window-*` — ROW BUDGETS RE-DERIVED against the
+1-row-per-block geometry, not just re-recorded — `fullscreen-*`, resume/attach replay). Then:
`npm run typecheck`; full `npx vitest run test/tui/` + `test/unit/`; `npm run test:resize-matrix`
(tmux; private socket only). Frame-parity check for A6: decode
`test/fixtures/upstream-frames/f1-tool-rendering/01-read-complete.ansi` rows 12-24 and assert our
equivalent sequence produces the same blank-row pattern (prompt/gap/tool/body/gap/spinner/gap/rule).
Report every budget you re-derived with the arithmetic in the task report.
