# bl10 T-MENU — dialog shell + /mcp browser + status-family entry points

> **For agentic workers:** REQUIRED SUB-SKILL: doperpowers:subagent-driven-development, task-by-task.
> Spec: `docs/superpowers/specs/2026-08-31-bl10-menus-click-spacing-design.md` (§2.1, D2–D5).
> Research (all canon cites resolved there — READ THE RELEVANT SECTION BEFORE EACH TASK):
> `.doperpowers/sdd/2026-08-31-bl10-round/research-slash-menus.md` (R1). Canon bundle:
> `~/claude-code-bundle/2.1.251/cli.pretty.js` (grep -n + sed windows only; 881k lines — never read whole).

**Goal:** finish the shared tabbed-dialog shell (canon `Pg`/`Zi`/`me`/keyhint bar), migrate the three
existing tabbed dialogs onto it, add the missing Auto mode permissions tab, route the status-family
commands into the Settings dialog, and build the `/mcp` navigation-stack browser.

**Global constraints** (bind every task):
- Commands from `harness/`: `npm run typecheck`, `npx vitest run test/tui/<file>` per-file; never bare
  `npm test`. TDD: red for the finding's reason before green. Commit per task, no Co-Authored-By.
- Canon literals verbatim (titles, keyhints, empty states) — when a literal is needed, grep the bundle
  at the cited line and transcribe exactly; never invent copy.
- No dialog-registry refactor (spec D4): dialogs keep the `useChat` state field + `ChatApp.tsx`
  overlayChain pattern.
- Do not touch `src/appserver/`, `reforge/`, or `ptc-surface/`.

### Task 1 — Shell: Tab pane + DialogFrame upgrade

**Files:** modify `src/tui/select/Tabs.tsx` (pane half; see its :5-7 comment recording the deliberate
skip — retire that comment), `src/tui/dialogs/DialogFrame.tsx` (frame upgrade), `src/tui/keys/bindings.ts`
(only if a new action name is needed for hints); create `src/tui/dialogs/keyhints.ts` (action→description
registry, canon `Ye` L568825: `confirm:no`→"cancel", `select:accept`→"select", `tabs:next`/`tabs:previous`
→"switch tab", etc. — transcribe the registry entries our scopes actually use). Test:
`test/tui/tabs-shell.test.tsx` (new), extend `test/tui/dialog-frame.test.tsx` if present else create.

**Produces (later tasks consume):** `<Tab title id?>` pane component exported from `select/Tabs.tsx`
rendering children only when active (canon `Zi` L122728); `Tabs` deriving its tab list from children
(`props.id ?? props.title`), supporting `defaultTab` (uncontrolled) and `selectedTab`+`onTabChange`
(controlled); `DialogFrame` props extended with `subtitle?`, `titleEnd?` (dim, right-aligned,
truncate-start), `onCancel?` (Esc binds in the frame), and an auto keyhint bar (≤4 hints derived from
the active key scope via `keyhints.ts`) replacing hand-written footers when `hintScope` is given.

Steps: failing tests first (pane shows only active child; controlled/uncontrolled switching; frame
renders subtitle/titleEnd; keyhint bar lists mapped hints for a scope; Esc fires onCancel) → implement
→ green → typecheck → commit.

### Task 2 — Migrate the three tabbed dialogs; Permissions Auto mode tab

**Files:** modify `src/tui/SettingsDialog.tsx` (:73-75 TABS/TAB_SPECS + strip :474 + body switch),
`src/tui/PermissionsDialog.tsx` (:61-63 same pattern), `src/tui/HelpDialog.tsx` (:91, :192). Tests:
`test/tui/settings-dialog.test.tsx`, `test/tui/permissions-dialog.test.tsx`, `test/tui/help-dialog.test.tsx`
(adjust; visual frames should be equivalent except deliberate upgrades: keyhint bar, the new tab).

Requirements: the migration DELETES the per-dialog TABS/TAB_SPECS/body-switch boilerplate (spec A4) —
each dialog becomes `<Tabs>` + `<Tab>` children on the upgraded frame. **Permissions (spec D12 —
canon order verified at cli.pretty.js:829914-829953): our existing five tabs ALREADY match canon's
order (Recently denied · Allow · Ask · Deny · Workspace minus Auto mode) — do NOT reorder; INSERT
"Auto mode" between Deny and Workspace.** The tab is display-only this round: grep canon chunk
`y7da6q4n` (~L826981-829986) for the automode tab's content shape and empty-state literal; surface
whatever auto-mode rule data our `permissionsModel.ts` / settings expose, else render canon's empty
state verbatim; no add/edit mutations. Pin the exact six-tab order, keyboard cycling across the new
tab, and narrow-pane geometry deliberately (cite spec D12 in test comments).

### Task 3 — Status-family entry points

**Files:** modify `src/tui/useChat.ts` command arms (`/status`, `/usage`, `/cost`, `/stats` — locate
by switch arm, ~:2245-2410) to open `SettingsDialog` with a `defaultTab` (`Status`/`Usage`/`Usage`/
`Stats`) instead of pushing formatted text; ADD `defaultTab` plumbing (verified absent — no
`defaultTab`/`initialTab` exists anywhere in SettingsDialog/useChat/ChatApp today); keep
`formatStatus`/`formatUsage`/etc. exported. Tests: command/dialog-opening suites + NEW
information-equivalence tests.

**Information-equivalence gate (spec D13 — an arm converts ONLY when its dialog loader preserves its
semantics; equivalence tests land BEFORE the arm is removed):**
- `/status` today performs a FRESH context measurement (`useChat.ts:~2254-2268`) while
  `fetchSettingsStatus` (`useChat.ts:~3078-3084`) reads possibly-absent/stale `ctxPct` — make the
  Status tab re-measure on open.
- `/cost` today renders cost, duration, code-change, and per-model usage detail (`commands.ts:314-349`)
  while the Usage tab renders only plan-rate-limit bars (`usageFormat.ts:49-54`) — every field the
  text form shows must be visible in the Usage/Stats panes before the text arm goes (where canon's
  pane lacks a field, keep it as recorded additive divergence per D13).
- `/stats` today emits an in-flight staleness disclaimer — the Stats tab carries it.
Equivalence tests: for each command, render the dialog tab and assert the presence of every
information element the text form emits today (enumerate from the formatters). `commands.ts`
summaries updated if they promise text output.

### Task 4 — /mcp browser

**Files:** create `src/tui/McpDialog.tsx` + `src/tui/mcpDialogModel.ts` (pure view-stack model:
states `list | server-menu | server-tools | server-tool-detail`, Esc pops one level, Enter drills —
canon router L582270); modify `src/tui/useChat.ts` (`/mcp` arm :2337 — dialog instead of
`formatMcpStatus` text when no args; keep `reconnect`/`toggle` arg forms text-based), `ChatApp.tsx`
overlayChain (one new arm). Tests: `test/tui/mcp-dialog.test.tsx` (new, model + render), reuse the
fake session data the existing `formatMcpStatus` tests use.

Requirements: **data source (spec D5 amended)** — a typed normalization of
`session.mcpServerStatus()` (see `session/chatSession.ts:16-29`; there is NO flattened session tool
inventory): the SDK's `McpServerStatus` rows already carry per-server `tools: {name, description?,
annotations?}[]` (`sdk.d.ts:1141-1151`) plus config/scope — the normalization lives in
`mcpDialogModel.ts` and the drill-downs derive from it. Root list = frame "Manage MCP servers",
subtitle `${n} servers`, grouped section headers only for groups our scopes distinguish (omit empty
groups), windowed list with `↑ N more above` / `↓ N more below` (transcribe canon literals, chunk
`j9x7bey3` L581310-582561), keyhint footer via the Task-1 bar. Server menu: the Type:/URL:/Command:/
Status: field block for fields the row carries (omit absent). Tools view from `row.tools`; tool
detail = name + description + annotations when present. **Geometry (spec D15):** the MCP dialog
joins `paneOwned` so transcript/task-panel/spinner unmount beside it; pass authoritative
rows/columns. Skip agent-server-menu/OAuth/show-all (spec D5). Esc semantics per A2. Tests: rich
local AND remote payloads, raw names/descriptions, absent/malformed fields, and
failed/needs-auth/pending/disabled states — not just empty and `{name,status}` rows.

### Task 5 — Battery + acceptance walk

Run: `npm run typecheck`; `npx vitest run test/tui/` (full tui suite); `npx vitest run test/unit/`;
walk spec acceptance A1–A4 item by item against rendered frames (ink-testing-library), recording each
in the task report. Fix or surface anything red. Commit.

> Merge note (spec D15): `ChatApp.tsx` is a declared reconciliation seam with T-SPACE — the
> post-merge battery (controller-run) adds a combined short-terminal + maximum-dock geometry test
> (MCP dialog open, spinner ticking, palette open) that this branch cannot run alone. Do not attempt
> it here.
