# bl10 T-CLICK — band-marker hit region for expanded blocks (plan v2)

> **For agentic workers:** REQUIRED SUB-SKILL: doperpowers:subagent-driven-development, task-by-task.
> Spec: `docs/superpowers/specs/2026-08-31-bl10-menus-click-spacing-design.md` (§2.3, D9 v2, D10, A5).
> Research (the full diagnosis — READ §1.3 and §2.5 BEFORE Task 1):
> `.doperpowers/sdd/2026-08-31-bl10-round/research-click-collapse.md` (R2).
> **Branches from post-T-MENU-AND-T-SPACE main** (spec D15): the fold-click frames sit atop T-SPACE's
> separators, and the band must provably exclude them.

**Goal:** an expanded block collapses on a click anywhere in its full-width band; a collapsed block
keeps the glyph-width bound. One explicit row-level band marker drives BOTH the full-width paint and
the widened hit region (spec D9 v2 — `item.expanded` is NOT a usable proxy: the absorbed-thinking
margin row carries `expanded:true`+`foldAnchor` at `toolRenderer.tsx:1061-1071`, and expanded headers
are tagged expanded while only result bodies get `withExpandedMarker` :561-587).

**Global constraints:** commands from `harness/`; typecheck + scoped vitest per task; TDD red-first;
the fold state machine (`useChat.ts:358/:369`) and dispatch (`ChatApp.tsx:1189-1192`) are CORRECT —
do not touch them. The 500 ms/±1-cell double-click word-select is canon parity — leave it (spec D10).
Spacing separators must never carry the band marker or a click target. Commit per task, no
Co-Authored-By.

### Task 1 — The band marker + full-width paint

**Files:** add a row-level `band` marker to the render-item shape (where `expanded`/`foldAnchor`
already live) set EXACTLY on rows composing an expanded block's visual band: the header row, result
body rows, the trailing pad row `withExpandedMarker` emits (`src/tui/toolRenderer.tsx:544-549`), and
every `expandedMemberItems` member row (:1073 region) — NOT on the absorbed-thinking leading blank
(:1069, it is a margin) and NOT on any T-SPACE separator. Paint: `src/tui/Line.tsx` (:103-132 applies
`bg` at text width) and/or the fullscreen painter so a marked row's background reaches the terminal
edge (canon paints a rectangle of styled spaces, cli.pretty.js:376156-376163). Hover suppression for
expanded items (`FullscreenViewport.tsx:927`) stays. **Tests:** on ONE rendered frame, assert
per-row-class band extent — header banded full-width, body banded full-width, pad row banded,
margin/separator rows unbanded (SGR-level assertions in the render/sgrFoldRow suites); expanded fold
cluster shows the band; collapsed rows unchanged.

### Task 2 — Hitmap widens from the marker + test flips

**Files:** `src/tui/FullscreenViewport.tsx` row constructors (:282 line rows, :315-316 gutter-block
body rows) carry the marker into the hit rows; a marked row's hit `width` becomes the full column
count; `clickTargetAt` (:444-463) needs no new branch — its `col > at.width` guard answers both
states. Check `src/tui/mouse/hitmap.ts:126-137` for a parallel bound. **Tests** in
`test/tui/fold-click.test.tsx`: FLIP :707 ("a click past the row's own text never collapses it") to
assert the expanded blank tail DOES collapse — comment cites spec D9 v2 and canon's
cellIsBlank/background-rectangle rule (cli.pretty.js L19169-19174, L372918, L376156); KEEP :696
(collapsed blank tail inert); add: expanded cluster member row's blank tail collapses the cluster;
a click on the SEPARATOR row directly above/below an expanded block does NOT toggle anything (the
D9-v2 regression the marker exists to prevent); paint extent == hit extent on the same frame (the
Task-1 frame assertions re-used against `clickTargetAt`). Red-first: the flipped test must fail
against pre-change code for the hit-region reason.

### Task 3 — Docs rule flip + battery + pty cell

Update `docs/parity/tui-ux.md` blank-tail rule (~:2209) to the asymmetric band form (collapsed:
glyph-only; expanded: full-width band; separators inert) citing R2; add the D10 double-click UX note
beside it. Battery: `npm run typecheck`; `npx vitest run test/tui/`; re-run the existing fold-click
pty cell(s) against the REAL binary (see `docs/parity/tui-ux.md:2209`'s bl4 cell for the recipe;
private tmux socket only, never kill-server) and add one cell case: expand a truncated result by
clicking it, then click its blank tail (well past end-of-text, >1 cell away or >500 ms later) and
assert byte-identical return to the collapsed frame. Record cell transcripts under
`.doperpowers/sdd/2026-08-31-bl10-round/`.
