# bl10 T-CLICK — asymmetric hit region for expanded blocks

> **For agentic workers:** REQUIRED SUB-SKILL: doperpowers:subagent-driven-development, task-by-task.
> Spec: `docs/superpowers/specs/2026-08-31-bl10-menus-click-spacing-design.md` (§2.3, D9–D10, A5).
> Research (the full diagnosis — READ §1.3 and §2.5 BEFORE Task 1):
> `.doperpowers/sdd/2026-08-31-bl10-round/research-click-collapse.md` (R2).
> **Branches from post-T-SPACE main** (fold-click frames sit atop the new spacing).

**Goal:** make an expanded block collapsible by clicking anywhere on its full-width band (canon's
background-rectangle rule), while a collapsed block keeps the glyph-width bound — and make the band
actually full-width and present on expanded fold clusters.

**Global constraints:** commands from `harness/`; typecheck + scoped vitest per task; TDD red-first;
the state machine (`useChat.ts:358/:369`) and dispatch (`ChatApp.tsx:1189-1192`) are CORRECT — do not
touch them. The double-click word-select behavior (500 ms/±1 cell) is canon parity — leave it alone
(spec D10). Commit per task, no Co-Authored-By.

### Task 1 — Hitmap: expanded rows get full-column width

**Files:** modify `src/tui/FullscreenViewport.tsx` row constructors (:282 line rows, :315-316
gutter-block body rows — both currently `width: min(gutter + stringWidth(text), columns)`, neither
reads `item.expanded`) so a row belonging to an expanded item carries `width: columns`; `clickTargetAt`
(:444-463) needs no new branch — its `col > at.width` guard then answers both states. Check
`src/tui/mouse/hitmap.ts:126-137` for a parallel bound. Tests in `test/tui/fold-click.test.tsx`:
FLIP :707 ("a click past the row's own text never collapses it") to assert the expanded blank tail
DOES collapse — with a comment citing spec D9 and canon's cellIsBlank/background-rectangle rule
(cli.pretty.js L19169-19174, L372918, L376156); KEEP :696 (collapsed blank tail inert) unchanged;
add the fold-cluster variant (expanded cluster member row's blank tail collapses the cluster).
Red-first: the flipped test must fail against current code for the hit-region reason.

### Task 2 — The band: full-width + on expanded clusters

**Files:** modify `src/tui/toolRenderer.tsx` `withExpandedMarker` (:544-549) and/or
`src/tui/Line.tsx` (:103-132 applies `bg` at text width) so the expanded band paints to the terminal
edge (canon paints a rectangle of styled spaces, cli.pretty.js:376156-376163); extend the band to
`expandedMemberItems` (:1073 region) so an expanded fold cluster shows it. Hover suppression for
expanded items (`FullscreenViewport.tsx:927`) stays — the band replaces hover feedback
(canon-faithful). Tests: band width assertions in the SGR/render tests (`sgrFoldRow`/render suites);
cluster-band presence in fold-click or fold-expand tests. Mind T-SPACE's new separator items: the
band must not paint onto the separator blank row above the block.

### Task 3 — Docs rule flip + battery + pty cell

Update `docs/parity/tui-ux.md` blank-tail rule (~:2209) to the asymmetric form (collapsed: glyph-only;
expanded: full-width band) citing R2; add the D10 double-click UX note beside it. Battery:
`npm run typecheck`; `npx vitest run test/tui/`; re-run the existing fold-click pty cell(s) against
the REAL binary (see `docs/parity/tui-ux.md:2209`'s bl4 cell for the recipe/location; private tmux
socket only, never kill-server) and add one cell case: expand a truncated result by clicking it, then
click its blank tail (well past end-of-text, >1 cell away from the first click or >500 ms later) and
assert byte-identical return to the collapsed frame. Record cell transcripts under the round dir.
