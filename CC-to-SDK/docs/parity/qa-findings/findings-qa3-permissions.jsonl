{"id": "qa3-01", "surface": "chrome", "severity": "P2-wrong", "title": "ccx launches in `auto` permission mode; Claude Code launches in manual (default) mode", "repro": ["fresh isolated HOME + scratch project", "launch ccx (no flags) / launch claude (no flags)", "read the footer mode chip"], "ccx": "footer `model claude-opus-5  mode auto (⇧Tab to cycle)` at first paint; shift+tab from that state goes to `mode default`, confirming the launch state really is auto. Consequence: the model's own classifier decides what runs. frames-qa3/qa3-01-ccx-bash-noconsult.txt", "claude": "footer `⏸ manual mode on · ? for shortcuts · ← for agents` at first paint; every mutating Bash call raises a dialog. frames-qa3/qa3-01-cc-bash-consult.txt", "suspects": "harness default permissionMode in resolveOptions / REPL launch defaults", "kind": "defect"}
{"id": "qa3-02", "surface": "chrome", "severity": "P2-wrong", "title": "ccx banner and footer disagree about the current permission mode at launch", "repro": ["launch ccx", "read banner line `model (default) · mode default` and footer `mode auto (⇧Tab to cycle)` in the same frame"], "ccx": "banner says `model  (default)   ·   mode  default`; footer says `mode auto (⇧Tab to cycle)`. Behaviour matches the footer (auto). frames-qa3/qa3-01-ccx-bash-noconsult.txt", "claude": "no equivalent contradiction — the welcome box carries no mode field and the footer is the single source of truth (`⏸ manual mode on`).", "kind": "defect"}
{"id": "qa3-03", "surface": "dialog", "severity": "P1-broken", "title": "ccx `git init` and `rm <file>` run with no consult where Claude Code gates both", "repro": ["launch both with no flags", "prompt: `Run the shell command: git init`", "later, in a project with the file present: `Run the shell command: rm qa3-marker.txt`"], "ccx": "both executed silently: `⏺ Bash(git init)` → output, and `⏺ Bash(rm qa3-marker.txt)` → `⎿ Done`. No dialog in either case. (Same session DID gate `touch qa3-marker.txt`, so it is the auto-mode classifier's judgement, not a missing dialog.) frames-qa3/qa3-01-ccx-bash-noconsult.txt, qa3-10-ccx-nomiddraft.txt", "claude": "`git init` → `Bash command / git init / Initialize a new git repository / This command requires approval / Do you want to proceed?`; `rm qa3-marker.txt` → same dialog shape with `Delete qa3-marker.txt`. frames-qa3/qa3-01-cc-bash-consult.txt, qa3-11-cc-middraft-reveal.txt", "suspects": "downstream of qa3-01 (auto launch mode) plus classifier divergence: ccx gates the harmless `touch` but allows the destructive `rm`", "kind": "defect"}
{"id": "qa3-04", "surface": "dialog", "severity": "P2-wrong", "title": "ccx's `No, and tell Claude what to do differently` denies immediately instead of collecting feedback", "repro": ["default mode, prompt a Write", "on the consult dialog press Down Down to reach `3. No`, press Tab to amend, press Enter"], "ccx": "transcript jumps straight to `⏺ Write(x.txt)` / `⎿  Error: User denied Write` and the model resumes streaming. No prompt for the promised feedback. frames-qa3/qa3-06-ccx-deny-nofeedback.txt", "claude": "transcript shows `⎿  Interrupted · What should Claude do instead?`, the turn parks, and the next composer submission is delivered as the denial reason (typed `Do not run it. Just reply exactly: DENY-OK` → `⏺ DENY-OK`). frames-qa3/qa3-04-cc-deny-feedback.txt", "kind": "defect"}
{"id": "qa3-05", "surface": "dialog", "severity": "P3-missing", "title": "ccx consult dialogs have no `Tab to amend` / `ctrl+e to explain` affordances in the footer, and Tab does nothing on the Yes row", "repro": ["open any consult dialog on both", "read footer", "press Tab with row 1 highlighted"], "ccx": "footer is only `esc cancel`. Tab on row 1 (`Yes`) leaves the label unchanged; Tab only works on row 3. No ctrl+e at all. frames-qa3/qa3-05-ccx-write-dialog.txt, qa3-07-ccx-bash-dontask.txt", "claude": "footer `Esc to cancel · Tab to amend · ctrl+e to explain`. Tab amends whichever row is highlighted: row 1 → `Yes, and tell Claude what to do next`, row 3 → `No, and tell Claude what to do differently`. ctrl+e expands the dialog with a full description, the model's stated intent, and a risk line (`Low risk: …`), and the footer flips to `ctrl+e to hide`. frames-qa3/qa3-02-cc-bash-explain.txt, qa3-03-cc-bash-tab-amend.txt", "kind": "gap", "version-drift?": true}
{"id": "qa3-06", "surface": "dialog", "severity": "P4-polish", "title": "ccx amended deny label has a double space: `No,  and tell Claude what to do differently`", "repro": ["consult dialog → Down Down → Tab", "read row 3"], "ccx": "`❯ 3. No,  and tell Claude what to do differently` (two spaces after the comma). frames-qa3/qa3-05-ccx-write-dialog.txt", "claude": "`❯ 3. No, and tell Claude what to do differently` (single space). frames-qa3/qa3-03-cc-bash-tab-amend.txt", "kind": "defect"}
{"id": "qa3-07", "surface": "dialog", "severity": "P4-polish", "title": "ccx Create-file dialog shows the raw body; Claude Code frames it as a numbered diff", "repro": ["default mode", "prompt: create a file named x.txt containing exactly HELLO using the Write tool"], "ccx": "` Create file / x.txt / HELLO` — content printed bare, no rules, no line numbers. frames-qa3/qa3-05-ccx-write-dialog.txt", "claude": "` Create file / x.txt` then a `╌╌╌` rule, `  1 HELLO`, a closing `╌╌╌` rule. frames-qa3/qa3-05-cc-write-dialog.txt", "kind": "defect"}
{"id": "qa3-08", "surface": "dialog", "severity": "P4-polish", "title": "ccx's don't-ask-again suggestion row is ungrammatical and conflates directory grant with command rule", "repro": ["default mode", "prompt: `Run the shell command: touch qa3-marker.txt`", "read row 2 of the dialog"], "ccx": "`2. Yes, and allow access to proj-ccx-8b1gAu/ and touch qa3-marker.txt commands`. frames-qa3/qa3-07-ccx-bash-dontask.txt", "claude": "two distinct shapes depending on the suggestion: `2. Yes, and don’t ask again for: git init *` (command-prefix rule) or `2. Yes, and always allow access to proj-cc-qc2niL/ from this project` (directory grant). Never both in one row. frames-qa3/qa3-01-cc-bash-consult.txt, qa3-07-cc-bash-dontask.txt", "kind": "defect"}
{"id": "qa3-09", "surface": "session", "severity": "P4-polish", "title": "PASS — don't-ask-again writes .claude/settings.local.json and the rule survives relaunch, on both sides", "repro": ["default/manual mode", "trigger a Bash consult, choose row 2", "cat <project>/.claude/settings.local.json", "quit, relaunch in the same project, repeat the same command"], "ccx": "wrote `{\"permissions\":{\"allow\":[\"Bash(touch qa3-marker.txt)\"]}}`; after relaunch the same command ran with no dialog. Note ccx persisted only the command rule and dropped the directory-access half of its own suggestion text.", "claude": "wrote `{\"permissions\":{\"allow\":[\"Bash(git init *)\"]}}`; after relaunch `git init` ran folded as `Ran 1 shell command` with no dialog. When the suggestion was the directory grant instead, nothing was written to settings.local.json — it landed as a Workspace entry and flipped the footer to `⏵⏵ accept edits on`.", "kind": "divergence-question"}
{"id": "qa3-10", "surface": "session", "severity": "P4-polish", "title": "PASS — mid-draft suppression and the idle reveal work on both", "repro": ["submit a prompt that will need a consult", "immediately type continuously (one char every 0.5 s) for ~10 s", "stop typing and wait ~3 s"], "ccx": "while typing: dim row ` Waiting for permission…` above the composer, draft intact, spinner line `✽ Spelunking… (13s …)` still shown. After idle the full Bash dialog replaces it; after answering, the draft `bbbb…` is restored. frames-qa3/qa3-12-ccx-middraft-waiting.txt, qa3-13-ccx-middraft-reveal.txt", "claude": "identical behaviour — ` Waiting for permission…`, footer shrinks to `⏸ manual mode on`, and the spinner line is replaced by `Running 1 shell command… / ⎿  $ rm qa3-marker.txt`. Reveal restores the dialog and the `aaaa…` draft survives the answer. frames-qa3/qa3-10-cc-middraft-waiting.txt, qa3-11-cc-middraft-reveal.txt", "kind": "divergence-question"}
{"id": "qa3-11", "surface": "panel", "severity": "P3-missing", "title": "ccx /permissions has no search box and its `Recently denied` tab lists user denials instead of classifier denials", "repro": ["/permissions on both", "read the Allow/Ask/Deny tabs and the Recently denied tab"], "ccx": "bordered box, same five tabs, but no `⌕ Search…` field on Allow/Ask/Deny and no row numbering. `Recently denied` is captioned `Commands recently denied by the auto mode classifier.` yet lists `Write(/private/tmp/.../x.txt)  by ccx-6158` — a dialog denial the user made by hand, contradicting its own caption. Allow tab does correctly show `Bash(touch qa3-marker.txt)  From project local settings`. frames-qa3/qa3-09-ccx-permissions.txt", "claude": "tabs `Recently denied  Allow  Ask  Deny  Workspace`; Allow/Ask/Deny each carry a `⌕ Search…` box and numbered rows (`1. Add a new rule…`); `Recently denied` stayed at `No recent denials. Commands denied by the auto mode classifier will appear here.` even after a hand denial. frames-qa3/qa3-08-cc-permissions-allow.txt, qa3-08b-cc-permissions-ask.txt", "kind": "defect"}
{"id": "qa3-12", "surface": "panel", "severity": "P4-polish", "title": "ccx /permissions omits `←/→ to switch` from the footer on the first tab, though the key works", "repro": ["/permissions on ccx", "read footer on `Recently denied`, then press Right and read it again"], "ccx": "first tab footer: `↑/↓ to navigate · Esc to cancel`. After Right: `↑/↓ to navigate · Enter to select · ←/→ to switch · Esc to cancel`. The hint appears only after you have already discovered the key. frames-qa3/qa3-09-ccx-permissions.txt", "claude": "`←/→ to switch · ↓ to select · Esc to cancel` on every tab including the first. frames-qa3/qa3-08-cc-permissions-allow.txt", "kind": "defect"}
{"id": "qa3-13", "surface": "transcript", "severity": "P3-missing", "title": "ccx prints no explainer when entering auto mode; Claude Code writes a four-line safety notice into the transcript", "repro": ["shift+tab until the footer reads auto mode", "read the transcript"], "ccx": "nothing is emitted — only the footer chip changes to `mode auto (⇧Tab to cycle)`.", "claude": "emits `…Claude reviews each command for destructive operations and prompt injection before executing. Actions Claude identifies as safe are executed, while actions Claude identifies as risky are blocked and Claude may try a different approach. Ideal for long-running tasks. Sessions are slightly more expensive. Claude can make mistakes that allow harmful commands to run, it's recommended to only use in isolated environments. Shift+Tab to change mode.`", "kind": "gap", "version-drift?": true}
{"id": "qa3-14", "surface": "chrome", "severity": "P3-missing", "title": "ccx has no bypass-permissions mode and rejects --dangerously-skip-permissions, so the bypass warning dialog does not exist", "repro": ["node harness/dist/cli/bin.js --dangerously-skip-permissions"], "ccx": "prints `ccx: unknown flag --dangerously-skip-permissions` and exits with status 2. Neither the shift+tab ladder (auto → default → acceptEdits → plan → auto) nor any flag reaches a bypass state.", "claude": "`claude --dangerously-skip-permissions` shows the full-screen `WARNING: Claude Code running in Bypass Permissions mode` consent gate whose default highlighted row is `1. No, exit`, and once consented the footer reads `⏵⏵ bypass permissions on (shift+tab to cycle)` (recipe §3.1 gate 4; re-confirmed by the seed key `skipDangerousModePermissionPrompt` being required in this run).", "kind": "gap"}
{"id": "qa3-15", "surface": "chrome", "severity": "P4-polish", "title": "PASS with one difference — the shift+tab ladder has the same four modes in the same order, but ccx starts one step further along", "repro": ["shift+tab repeatedly on both, reading the footer each press"], "ccx": "auto → default → acceptEdits → plan → auto. Launch state = auto.", "claude": "manual → accept edits → plan → auto → manual. Launch state = manual. Same cycle, different entry point (see qa3-01).", "kind": "divergence-question"}
{"id": "qa3-16", "surface": "dialog", "severity": "P2-wrong", "title": "ccx plan-approval modal is missing the `Ready to code?` chrome, the plan frame, the plan file, and ctrl+g", "repro": ["shift+tab to plan mode on both", "prompt: `Plan how to add a hello() function to a new file greet.js. Keep it very short.`", "read the approval modal"], "ccx": "plan renders as ordinary transcript text; modal is just ` Claude has written up a plan and is ready to execute. Would you like to proceed? / ❯ 1. Yes, auto-accept edits / 2. Yes, manually approve edits / 3. Tell Claude what to change / shift+tab to approve`. No heading, no frame, no file path, no editor key. frames-qa3/qa3-14-ccx-plan-modal.txt", "claude": "`▔▔▔` top rule, ` Ready to code?`, ` Here is Claude's plan:`, the plan boxed between `╌╌╌` rules, then `❯ 1. Yes, and use auto mode / 2. Yes, manually approve edits / 3. Tell Claude what to change / shift+tab to approve with this feedback`, and a footer `ctrl+g to edit in VS Code · ~/.claude/plans/plan-how-to-add-ancient-robin.md`. frames-qa3/qa3-14-cc-plan-modal.txt", "kind": "defect", "version-drift?": true}
{"id": "qa3-17", "surface": "dialog", "severity": "P2-wrong", "title": "ccx plan option 1 says `auto-accept edits` where Claude Code says `use auto mode` — different permission mode granted", "repro": ["plan mode approval modal on both", "read row 1"], "ccx": "`1. Yes, auto-accept edits` (acceptEdits).", "claude": "`1. Yes, and use auto mode` (auto — the classifier mode, which also covers Bash, not just edits). The two grants are not equivalent, so the same keystroke leaves the session under different permission rules.", "kind": "defect"}
{"id": "qa3-18", "surface": "dialog", "severity": "P2-wrong", "title": "ccx `Tell Claude what to change` rejects the plan immediately instead of parking for feedback", "repro": ["plan mode approval modal", "Down Down → Enter on `3. Tell Claude what to change`"], "ccx": "transcript immediately shows `⎿  Error: User rejected the plan. Continue planning.` and the model resumes streaming (`⏺ The plan was rejected without a note, so let me pin down the two choices I made by default.`). The user never got to say what to change. frames-qa3/qa3-15-ccx-plan-reject-nofeedback.txt", "claude": "the turn ends (`✻ Cogitated for 41s`), the plan stays boxed in the transcript, and the composer is handed back so the next submission carries the feedback. An empty Enter at that point is a no-op. frames-qa3/qa3-15-cc-plan-reject-parked.txt", "suspects": "same root cause as qa3-04 — amended/feedback branches fire the decision without collecting text", "kind": "defect"}
{"id": "qa3-19", "surface": "dialog", "severity": "P3-missing", "title": "ccx AskUserQuestion has no question tab bar, no review-and-submit step, and no `Chat about this` escape", "repro": ["prompt: `Use the AskUserQuestion tool to ask me two questions, each with three options.`", "answer question 1, then question 2"], "ccx": "bordered box headed `[Modules] Which module style should greet.js use?  [1/2]`; answering advances straight to `[2/2]`, and answering that submits with no review. Options render label and description on one wrapped line. Rows are 1–3 plus `4. Type something.`; there is no `Chat about this` row and no way to go back to question 1. Footer `↑↓/j/k · number · enter · esc decline`. frames-qa3/qa3-16-ccx-askuserquestion.txt, qa3-17-ccx-auq-q2.txt", "claude": "tab bar `←  ☐ Module format  ☐ Behavior  ✔ Submit  →` with per-question checkboxes that fill in (`☒`) as you answer; options are two-line (label, then indented description); rows 1–3, `4. Type something.`, a rule, then `5. Chat about this`; after the last question a `Review your answers` screen lists every question with its `→ answer` and asks `Ready to submit your answers? ❯ 1. Submit answers / 2. Cancel`. Footer `Enter to select · Tab/Arrow keys to navigate · Esc to cancel`. frames-qa3/qa3-16-cc-askuserquestion.txt, qa3-17-cc-auq-q2.txt, qa3-18-cc-auq-review-submit.txt", "kind": "gap", "version-drift?": true}
