# Parity — 42a-utils-long-tail

| id | feature | verdict | SDK surface | bridge / gap | phase | conf | snap |
|---|---|---|---|---|---|---|---|
| 42a.1 | Internal helper utilities (string/path/fs/async/data-structure) | 🏗 build | — | Generic engineering helpers with no parity meaning; the SDK's host runtime (Node/Bun + std libs) and the CC binary provide equivalents. Reimplemented as needed; not a parity surface. ~250 utils rolled into this row. | Pnon-goal | inferred | feb |
| 42a.2 | Subsystem-entry-point utils owned by sibling specs | ✅ provided | Covered by their owning clusters: permissions (C3), agent/team (C5), MCP/skills/modes (C6), services (C8/C9), modes (C10) | These ~120 files implement features whose SDK verdicts live in other clusters (bash permissions, MCP, model selection, hooks, plugins). Rolled up here as cross-references, not re-verdicted; their parity is owned by the consuming-subsystem rows. | P1 | inferred | feb |
| 42a.3 | User-facing session utilities (search / list / cross-project resume) | ✅ provided | listSessions() + getSessionMessages() expose session enumeration/transcript read; listSessionsImpl is explicitly the SDK-facing implementation per spec 42a | listSessionsImpl is documented as the bootstrap-free Agent SDK listSessions backing, so this is a direct SDK surface. Semantic transcript search (agenticSessionSearch) is not separately exposed — consumers run their own search over getSessionMessages output. | P1 | doc | feb |
| 42a.4 | User-facing doctor / diagnostics + installer utilities | 🏗 build | — | These back the CC binary's own install/diagnostics commands. The SDK consumes a Claude Code executable (pathToClaudeCodeExecutable) but does not surface doctor/installer/heapdump as APIs; an SDK host owns its own deployment health. | Pnon-goal | inferred | feb |
| 42a.5 | User-facing ANSI/terminal rendering + export utilities | 🏗 build | — | Terminal-output rendering and export are CLI/Ink-UI concerns (overlaps C11). The headless SDK emits structured messages; visual rendering/export is the consumer's responsibility. | Pnon-goal | inferred | feb |
