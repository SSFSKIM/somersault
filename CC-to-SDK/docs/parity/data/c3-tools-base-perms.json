[
  {"id":"08.1","area":"08-tool-base-registry","feature":"Tool<Input,Output,P> interface contract","what":"CC's single ~60-field Tool contract every capability implements (name, inputSchema, isEnabled, isReadOnly, isConcurrencySafe, checkPermissions, call, prompt, description, render*, mapToolResultToToolResultBlockParam, maxResultSizeChars). The SDK has no equivalent public 'Tool' object type — third-party tools are authored via the much smaller MCP `tool()` factory (name, description, inputSchema Zod, handler) and built-ins are opaque.","ccSource":"spec 08 §3.1; src/Tool.ts:362-695","verdict":"configurable","sdkSurface":"tool() + createSdkMcpServer + sdk-tools.d.ts input schemas","bridge":"SDK external tools are MCP-shaped (`tool(name, desc, zodSchema, handler, extras)`); the rich CC fields (isReadOnly, isConcurrencySafe, render*, checkPermissions, maxResultSizeChars) are NOT author-settable — only `annotations`, `searchHint`, `alwaysLoad` extras exist. Built-in CC tools are reachable only through the `tools` preset, not re-definable.","targetPhase":"2","confidence":"verified","snapshot":"feb"},
  {"id":"08.2","area":"08-tool-base-registry","feature":"buildTool() factory + TOOL_DEFAULTS (fail-closed defaults)","what":"CC's canonical constructor applies safe defaults for 7 DefaultableToolKeys (isEnabled=true, isConcurrencySafe=false, isReadOnly=false, isDestructive=false, checkPermissions=allow, toAutoClassifierInput='', userFacingName=name). The SDK's `tool()` factory has no defaultable-keys concept; concurrency/read-only/destructive classification is internal to built-ins.","ccSource":"spec 08 §3.2, §5.5, §6.1; src/Tool.ts:757-792","verdict":"build","sdkSurface":"","bridge":"No SDK surface lets an author declare isReadOnly/isConcurrencySafe/isDestructive on a custom tool; these drive CC's auto-edit/accept-edits gating and would have to be re-derived from `annotations` (readOnlyHint etc.) if we re-expose them.","targetPhase":"2","confidence":"inferred","snapshot":"feb"},
  {"id":"08.3","area":"08-tool-base-registry","feature":"Tool registry assembly (getAllBaseTools → getTools → assembleToolPool → getMergedTools)","what":"CC's 4-stage pipeline produces the per-request toolset: exhaustive base list → deny-filter+isEnabled → built-ins(sorted)+MCP(sorted) uniqBy name → flat merge for token counting. The SDK hides this entirely; clients only choose a base set via `tools` and add MCP tools via `mcpServers`.","ccSource":"spec 08 §3.3, §5.1-5.4; src/tools.ts:179-389","verdict":"provided","sdkSurface":"tools (preset 'claude_code' | []) + mcpServers + strictMcpConfig","bridge":"Pipeline is engine-internal; the SDK exposes only the inputs (`tools`, `mcpServers`) and the result is opaque. No client control over partition order or merge strategy.","targetPhase":"non-goal","confidence":"inferred","snapshot":"feb"},
  {"id":"08.4","area":"08-tool-base-registry","feature":"Tool-order prompt-cache invariant (StatSig global system caching)","what":"getAllBaseTools() order is hand-maintained to match the server `claude_code_global_system_caching` config so the system prompt caches across users; reordering invalidates global cache. assembleToolPool keeps built-ins as a sorted contiguous prefix so the cache breakpoint stays stable when MCP tools are added.","ccSource":"spec 08 §5.3, §6.5; src/tools.ts:191-192,354-365","verdict":"provided","sdkSurface":"tools preset 'claude_code'","bridge":"This is an internal performance invariant maintained by the SDK's bundled Claude Code binary; not a client concern and not exposed. Custom tool ordering cannot be influenced.","targetPhase":"non-goal","confidence":"inferred","snapshot":"feb"},
  {"id":"08.5","area":"08-tool-base-registry","feature":"Tool gating / availability (isEnabled + feature/env flags)","what":"~30 tools are conditionally present via feature() build gates, env vars (USER_TYPE, CLAUDE_CODE_SIMPLE, ENABLE_LSP_TOOL), and runtime predicates (isTodoV2Enabled, isWorktreeModeEnabled, isToolSearchEnabledOptimistic). The SDK exposes a fixed built-in set plus opt-ins for some surfaces (skills, sandbox, betas) but no per-tool feature-flag dial.","ccSource":"spec 08 §8.1-8.3; src/tools.ts gates","verdict":"configurable","sdkSurface":"tools + allowedTools + disallowedTools + skills + betas + extraArgs","bridge":"Coarse gating maps to `tools`/`allowedTools`/`disallowedTools`; CC's fine-grained build/env flags (USER_TYPE=ant tools, REPLTool, ConfigTool) are not reachable. `extraArgs` can pass raw CLI flags for anything the binary still honors.","targetPhase":"2","confidence":"inferred","snapshot":"feb"},
  {"id":"08.6","area":"08-tool-base-registry","feature":"Tool deny-rule filtering before model sees tool (filterToolsByDenyRules)","what":"Deny rules (incl. MCP server-prefix `mcp__server`) strip whole tools/servers from the pool BEFORE they are sent to the model, using the same matcher as the runtime permission check. The SDK's `disallowedTools` removes tools from context similarly.","ccSource":"spec 08 §5.4, §11; src/tools.ts filterToolsByDenyRules","verdict":"provided","sdkSurface":"disallowedTools + settings permissions.deny rules","bridge":"`disallowedTools: ['mcp__server']` and settings `permissions.deny` both strip tools pre-model, matching CC's deny-filter. The unified 'same matcher at registry and runtime' guarantee is internal but observably equivalent.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"08.7","area":"08-tool-base-registry","feature":"Deferred tool loading discipline (shouldDefer / alwaysLoad / ToolSearch)","what":"Tools with shouldDefer=true are sent with defer_loading:true so their schema is omitted from the initial prompt; the model must call ToolSearch to discover them. alwaysLoad overrides defer; MCP tools set alwaysLoad via _meta['anthropic/alwaysLoad'].","ccSource":"spec 08 §5.8, §11; src/tools.ts ToolSearch gate","verdict":"provided","sdkSurface":"tool() extras.alwaysLoad + searchHint + createSdkMcpServer alwaysLoad + McpServerConfig.alwaysLoad","bridge":"SDK author API exposes exactly the two knobs: `alwaysLoad` (force-include) and `searchHint` (ToolSearch keyword), plus per-MCP-server `alwaysLoad`. The defer/ToolSearch mechanism itself is engine-internal and on by default.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"08.8","area":"08-tool-base-registry","feature":"Tool aliases (toolMatchesName / Tool.aliases)","what":"A tool may carry `aliases` (e.g. TaskOutputTool aliases AgentOutputTool/BashOutputTool); findToolByName/toolMatchesName resolve a name to primary-or-alias. The SDK exposes a single-hop `toolAliases` redirect map applied before name resolution (e.g. redirect a built-in name to an MCP tool).","ccSource":"spec 08 §3.4, §5.1; src/Tool.ts toolMatchesName, tools.ts TaskOutputTool aliases","verdict":"provided","sdkSurface":"toolAliases (Record<string,string>)","bridge":"CC aliases are tool-declared (intrinsic, many→one); the SDK `toolAliases` is caller-supplied (config-time, single-hop, name→name). Semantics differ: CC's are baked-in alias lists; SDK's is an external redirect table. Same outcome (name resolution) but opposite authorship.","targetPhase":"2","confidence":"doc","snapshot":"feb"},
  {"id":"08.9","area":"08-tool-base-registry","feature":"Tool presets (TOOL_PRESETS = ['default'])","what":"CC's parseToolPreset/getToolsForDefaultPreset resolve a named preset to a tool-name list; only 'default' exists today. The SDK has the parallel `tools: { type:'preset', preset:'claude_code' }` selecting the full built-in set, and `tools: []` disabling all.","ccSource":"spec 08 §6.3; src/tools.ts:161-170","verdict":"provided","sdkSurface":"tools preset:'claude_code'","bridge":"CC's preset name is 'default'; the SDK's is 'claude_code'. Both are open enums for one preset. SDK additionally supports `tools: []` (disable all built-ins), which CC approximates via CLAUDE_CODE_SIMPLE / deny rules.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"08.10","area":"08-tool-base-registry","feature":"ToolUseContext god-object threading (~74 fields)","what":"Every CC tool call receives a ~74-field context (appState getters, mcpClients, handleElicitation, setToolJSX, agentId, denialTracking, contentReplacementState, etc.). The SDK never exposes this; custom MCP-tool handlers get only `(args, extra)` where extra carries limited request metadata.","ccSource":"spec 08 §4.1; src/Tool.ts:158-300","verdict":"build","sdkSurface":"","bridge":"None of CC's per-call context (file-state cache, app-state mutators, elicitation handler, tool-JSX setter, agent identity) is surfaced to SDK custom tools. Authors get the tool input plus an `extra` with the abort signal / request id only.","targetPhase":"non-goal","confidence":"inferred","snapshot":"feb"},
  {"id":"08.11","area":"08-tool-base-registry","feature":"ToolResult shape (data + newMessages + contextModifier + mcpMeta)","what":"CC tool results can inject newMessages, mutate context via contextModifier (non-concurrency-safe only), and carry mcpMeta (_meta/structuredContent). SDK MCP tools return a standard `CallToolResult` (content blocks + optional structuredContent/_meta) with no context-mutation or message-injection hook.","ccSource":"spec 08 §4.3; src/Tool.ts ToolResult","verdict":"configurable","sdkSurface":"tool() handler → CallToolResult (content + structuredContent + _meta)","bridge":"structuredContent/_meta map to CC's mcpMeta. CC's newMessages (inject extra conversation turns) and contextModifier (rewrite ToolUseContext) have no SDK equivalent for custom tools.","targetPhase":"2","confidence":"inferred","snapshot":"feb"},
  {"id":"08.12","area":"08-tool-base-registry","feature":"maxResultSizeChars disk-overflow threshold","what":"Each CC tool declares maxResultSizeChars; oversized results are persisted to disk and only a path-preview is returned to the model (FileReadTool overrides to Infinity). The SDK has no author-settable per-tool overflow threshold for custom tools.","ccSource":"spec 08 §11 checklist; src/Tool.ts maxResultSizeChars","verdict":"build","sdkSurface":"","bridge":"Custom SDK tools cannot set an overflow threshold; the engine applies its own large-result handling. No public knob.","targetPhase":"3","confidence":"inferred","snapshot":"feb"},
  {"id":"08.13","area":"08-tool-base-registry","feature":"CLAUDE_CODE_SIMPLE minimal toolset variant","what":"With CLAUDE_CODE_SIMPLE set, the toolset collapses to [Bash, Read, Edit] (+coordinator/REPL delta). The SDK reproduces a minimal toolset declaratively via `tools: []` plus selectively re-adding built-ins, or `allowedTools`.","ccSource":"spec 08 §5.2, §8.2; src/tools.ts:273-298","verdict":"configurable","sdkSurface":"tools ([] or explicit list) + allowedTools","bridge":"SDK uses an explicit tool list instead of an env flag; the exact [Bash,Read,Edit] set is expressible, but the coordinator/REPL auto-delta behavior is engine-internal.","targetPhase":"2","confidence":"inferred","snapshot":"feb"},
  {"id":"08.14","area":"08-tool-base-registry","feature":"MCP tool integration into the pool (assembleToolPool mcpTools param)","what":"MCP tools enter via the mcpTools parameter (from appState.mcp.tools after server connect), sorted as a separate partition after built-ins; built-in wins on name conflict (uniqBy). The SDK takes MCP servers via `mcpServers` and merges their tools automatically.","ccSource":"spec 08 §5.3, §12 Q3; src/tools.ts assembleToolPool","verdict":"provided","sdkSurface":"mcpServers + createSdkMcpServer + strictMcpConfig","bridge":"SDK accepts stdio/sse/http/sdk MCP server configs and surfaces their tools (prefixed `mcp__<server>__<tool>`); name-conflict resolution and partition ordering are engine-internal but behaviorally match (built-in precedence).","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"08.15","area":"08-tool-base-registry","feature":"Per-server / per-agent disallowed-tool sets","what":"CC has ALL_AGENT_DISALLOWED_TOOLS, CUSTOM_AGENT_DISALLOWED_TOOLS, ASYNC_AGENT_ALLOWED_TOOLS, COORDINATOR_MODE_ALLOWED_TOOLS governing which tools each agent context may use. The SDK exposes per-agent `tools`/`disallowedTools` on AgentDefinition.","ccSource":"spec 08 §6.4; constants/tools.js","verdict":"provided","sdkSurface":"AgentDefinition.tools + AgentDefinition.disallowedTools","bridge":"SDK per-agent allow/deny lists map to CC's agent tool-availability constants; MCP server-level specs in AgentDefinition.disallowedTools remove whole servers, matching CC's prefix-deny semantics.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.1","area":"09-permission-system","feature":"allow/deny/ask three-behavior model","what":"CC's PermissionBehavior is 'allow'|'deny'|'ask'; checkPermissions and rules resolve to one of these (plus internal 'passthrough'→'ask'). The SDK's PermissionResult is discriminated only on 'allow'|'deny' for the canUseTool return, but its broader PermissionBehavior type also includes 'ask', and HookPermissionDecision adds 'defer'.","ccSource":"spec 09 §4.2,§4.4; src/types/permissions.ts:44,174-266","verdict":"provided","sdkSurface":"canUseTool → PermissionResult (behavior allow|deny) + PermissionBehavior 'allow|deny|ask' + HookPermissionDecision adds 'defer'","bridge":"KEY DIVERGENCE: CC's checkPermissions can return 'ask' directly; the SDK canUseTool callback returns only allow/deny — 'ask' is expressed by NOT pre-allowing (let the engine prompt) or via settings `ask` rules. CC's internal 'passthrough' has no SDK analogue (it always converts to ask).","targetPhase":"1","confidence":"verified","snapshot":"feb"},
  {"id":"09.2","area":"09-permission-system","feature":"Master decision-tree ordering (deny → ask → tool-check → mode-bypass → allow-rule → ask)","what":"hasPermissionsToUseToolInner runs a fixed-order tree: 1a tool-wide deny, 1b tool-wide ask (sandbox auto-allow exception), 1c tool.checkPermissions, 1d-1g tool-deny/interaction/content-ask/safety-check short-circuits, 2a mode bypass, 2b tool-wide allow, 3 passthrough→ask. The SDK runs an equivalent but undocumented internal precedence; canUseTool fires only after settings rules and modes are consulted.","ccSource":"spec 09 §6.1; src/utils/permissions/permissions.ts:1158-1319,473-956","verdict":"provided","sdkSurface":"settings permissions (deny>ask>allow) + permissionMode + canUseTool callback","bridge":"The exact CC branch order (deny-first, safety-checks bypass-immune) is engine-internal. SDK clients observe: deny rules win, then ask rules, then allow rules, then mode, then canUseTool. Reproducing CC's precise short-circuits (1e requiresUserInteraction, 1g safetyCheck immune to bypass) is not separately configurable.","targetPhase":"1","confidence":"inferred","snapshot":"feb"},
  {"id":"09.3","area":"09-permission-system","feature":"Permission modes (default/acceptEdits/bypassPermissions/dontAsk/plan/auto)","what":"CC's user-addressable modes via permissions.defaultMode / --permission-mode. Mode gates the decision tree: bypassPermissions/plan-with-bypass auto-allow, dontAsk converts ask→deny, auto runs the YOLO classifier, acceptEdits auto-accepts edits. The SDK PermissionMode enum is the same 6 values plus the same dontAsk/auto.","ccSource":"spec 09 §4.1,§6.1; src/types/permissions.ts:16-38","verdict":"provided","sdkSurface":"permissionMode (default|acceptEdits|bypassPermissions|plan|dontAsk|auto) + Query.setPermissionMode + planModeInstructions","bridge":"1:1 on the 6 external modes. bypassPermissions additionally requires `allowDangerouslySkipPermissions:true` in the SDK (CC gates it via isBypassPermissionsModeAvailable). `auto` is CC's TRANSCRIPT_CLASSIFIER-gated YOLO mode — SDK exposes it but the classifier is server/engine-side.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.4","area":"09-permission-system","feature":"'bubble' mode for forked subagents","what":"CC's runtime-only 'bubble' mode (excluded from the Zod user-set) makes a forked subagent bubble its permission prompts up to the parent session rather than prompting locally or auto-rejecting. Used at forkSubagent.ts:67/runAgent.ts:430-443.","ccSource":"spec 09 §4.1 Discrepancy, §12.1; src/tools/AgentTool/forkSubagent.ts:67","verdict":"not-possible","sdkSurface":"","bridge":"No SDK PermissionMode value 'bubble'; the SDK enum is the 6 user-addressable modes only. Cross-subagent prompt bubbling to a parent is an internal coordinator concern not exposed to SDK clients. AgentDefinition.permissionMode accepts only the public enum.","targetPhase":"3","confidence":"doc","snapshot":"feb"},
  {"id":"09.5","area":"09-permission-system","feature":"Permission rule string syntax (ToolName / ToolName(content), escaping)","what":"CC rules are strings `ToolName` or `ToolName(content)` with escaped parens (\\( \\)) and backslashes; empty/`*` content collapse to a tool-wide rule. permissionRuleValueFromString/ToString round-trip with load-bearing escape order. The SDK stores rules as PermissionRuleValue { toolName, ruleContent? } objects and settings strings of the same `Tool(content)` form.","ccSource":"spec 09 §5.1; src/utils/permissions/permissionRuleParser.ts:93-152","verdict":"provided","sdkSurface":"settings permissions allow/deny/ask (string rules) + PermissionUpdate rules: PermissionRuleValue[]","bridge":"KEY SYNTAX MAP: CC settings.json uses the same `Tool(content)` string grammar; the SDK PermissionUpdate carries structured `{toolName, ruleContent}` objects instead of strings. A bridge must parse/serialize between CC's escaped-string form and the SDK's object form (the SDK's settings layer still accepts the string form on disk).","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.6","area":"09-permission-system","feature":"Legacy tool-name aliasing in rules (Task→Agent, KillShell→TaskStop, etc.)","what":"permissionRuleParser normalizes legacy names via LEGACY_TOOL_NAME_ALIASES (Task→Agent, KillShell→TaskStop, Agent/BashOutputTool→TaskOutput, Brief→BriefTool) so old rules keep matching renamed tools. The SDK's `toolAliases` provides caller-defined name redirection but no built-in legacy alias table for permission rules.","ccSource":"spec 09 §5.1,§6.10; src/utils/permissions/permissionRuleParser.ts:21-29","verdict":"configurable","sdkSurface":"toolAliases (Record<string,string>)","bridge":"CC's legacy aliases are hardcoded and rule-parser-internal; the SDK requires the caller to supply equivalent mappings via `toolAliases` if they want old rule names to resolve. Not automatic.","targetPhase":"2","confidence":"inferred","snapshot":"feb"},
  {"id":"09.7","area":"09-permission-system","feature":"Wildcard / prefix / exact rule matching (shellRuleMatching)","what":"CC's matcher: `prefix:*` → prefix rule, unescaped `*` → wildcard regex (dotAll, sentinel-protected escapes, single-trailing-wildcard space rule so `git *` matches bare `git`), else exact. Used for Bash command rules and tool content. The SDK settings rules support the same `Tool(prefix:*)` / `Tool(pattern*)` gitignore-style matching on disk.","ccSource":"spec 09 §5.2; src/utils/permissions/shellRuleMatching.ts:90-184","verdict":"provided","sdkSurface":"settings permissions rules (gitignore-style patterns, e.g. Bash(git push:*))","bridge":"The SDK's bundled engine applies the same matcher (it IS Claude Code); CC's exact wildcard semantics (trailing-space rule, dotAll) are not separately documented but reproduced by the same binary. canUseTool callbacks must implement their OWN matching for any custom logic — the matcher is not exported.","targetPhase":"1","confidence":"inferred","snapshot":"feb"},
  {"id":"09.8","area":"09-permission-system","feature":"MCP server-prefix rule matching (mcp__server matches mcp__server__tool)","what":"toolMatchesRule: a rule `mcp__server1` matches every tool `mcp__server1__*`; wildcard `mcp__server1__*` also covers them. Honors CLAUDE_AGENT_SDK_MCP_NO_PREFIX so MCP rules don't shadow builtins. The SDK supports the same server-level rule form in allowedTools/disallowedTools/settings.","ccSource":"spec 09 §5.3; src/utils/permissions/permissions.ts:238-269","verdict":"provided","sdkSurface":"allowedTools/disallowedTools 'mcp__server' + settings permissions mcp rules","bridge":"Server-prefix denial works identically (strips whole server). The CLAUDE_AGENT_SDK_MCP_NO_PREFIX env that controls prefixing is the same env the SDK respects (it spawns the CC binary).","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.9","area":"09-permission-system","feature":"PermissionRuleSource precedence (policy/flag/cli/command/settings/session)","what":"Rules carry a source: userSettings|projectSettings|localSettings|flagSettings|policySettings|cliArg|command|session. policy/flag/command are read-only; only user/project/local persist. The SDK's ResolvedSettingSource = SettingSource | 'managed' | 'flag', and PermissionUpdateDestination is the writable subset.","ccSource":"spec 09 §3,§4.3; src/types/permissions.ts:54-79","verdict":"provided","sdkSurface":"settingSources (user|project|local) + managedSettings + settings (flag tier) + PermissionUpdateDestination","bridge":"CC's 8 sources map to SDK tiers: policySettings≈managedSettings, flagSettings≈`settings` option/'flag', cliArg≈'cliArg' destination, command/session≈session-scoped. SDK's `managed` is read-only/restrictive-only, matching CC's policy read-only rule. CC's `command` source (rule added by a slash command) has no direct SDK destination.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.10","area":"09-permission-system","feature":"PermissionUpdate variants (addRules/replaceRules/removeRules/setMode/add+removeDirectories)","what":"CC's PermissionUpdate union mutates a ToolPermissionContext purely; setMode can only set the 5 external modes (not auto/bubble). applyPermissionUpdate is pure; persistPermissionUpdate writes only to editable sources. The SDK's PermissionUpdate is the SAME union (addRules/replaceRules/removeRules/setMode/addDirectories/removeDirectories).","ccSource":"spec 09 §4.3,§5.4; src/types/permissions.ts:88-131","verdict":"provided","sdkSurface":"PermissionUpdate[] (in PermissionResult.updatedPermissions, canUseTool suggestions, hook output)","bridge":"Near-identical type. SDK exposes PermissionUpdate via PermissionResult.updatedPermissions (allow) and canUseTool options.suggestions. setMode restricted to external modes in both. CC's persistence-source gating (supportsPersistence) is mirrored by the SDK's PermissionUpdateDestination enum.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.11","area":"09-permission-system","feature":"Permission persistence to settings + hot-reload sync","what":"persistPermissionUpdate writes permissions.{allow,deny,ask}/defaultMode to user/project/local settings.json; syncPermissionRulesFromDisk clears disk-source rules then re-applies on settings-file change (so deleted rules don't go stale). The SDK persists via the same settings.json files and resolveSettings/applyFlagSettings.","ccSource":"spec 09 §5.4,§8; src/utils/permissions/PermissionUpdate.ts:222-342","verdict":"provided","sdkSurface":"settings/settingSources + Query.applyFlagSettings + resolveSettings + PermissionUpdateDestination","bridge":"Persistence destinations match (user/project/local settings.json). SDK's `applyFlagSettings` updates the flag tier at runtime (analogous to CC's session/flag rules); on-disk hot-reload is engine-internal but the same files back both.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.12","area":"09-permission-system","feature":"Per-tool checkPermissions override (content-specific allow/ask/deny)","what":"Each tool's checkPermissions inspects parsed input and can return content-specific ask/deny (e.g. Bash command rules, file-path safety). The pipeline layers tool-wide rules+modes around it. The SDK has no per-tool checkPermissions hook for built-ins; the canUseTool callback is the single content-aware gate the client controls.","ccSource":"spec 09 §1,§6.1 step1c; src/Tool.ts checkPermissions, spec 08 §3.1","verdict":"provided","sdkSurface":"canUseTool callback (toolName, input, options) → PermissionResult","bridge":"KEY DIVERGENCE: CC distributes content-aware logic across each tool's checkPermissions; the SDK centralizes it in ONE canUseTool callback that sees (toolName, input). Built-in CC tools' baked-in checks (Bash classifier, file-path safety) are NOT individually overridable from the SDK — only globally wrappable via canUseTool.","targetPhase":"1","confidence":"verified","snapshot":"feb"},
  {"id":"09.13","area":"09-permission-system","feature":"Safety-check ask (bypass-immune sensitive paths .git/.claude/.vscode/shell-configs)","what":"Step 1g: a safetyCheck 'ask' on sensitive paths is immune to bypassPermissions; classifierApprovable flag controls whether auto-mode may approve. Even bypass mode prompts for these. The SDK has no exposed sensitive-path carve-out toggle.","ccSource":"spec 09 §4.5,§6.1 step1g; src/utils/permissions/permissions.ts:1158-1319","verdict":"build","sdkSurface":"","bridge":"The bypass-immune sensitive-path behavior is baked into the CC binary's built-in tools. SDK clients cannot configure which paths are safety-checked nor disable the carve-out; a canUseTool callback could re-implement equivalent checks but cannot reach the engine's internal safetyCheck.","targetPhase":"3","confidence":"inferred","snapshot":"feb"},
  {"id":"09.14","area":"09-permission-system","feature":"Sandbox auto-allow / sandbox-override permission path","what":"Step 1b exception: when Bash is sandboxed and isAutoAllowBashIfSandboxedEnabled, an ask rule falls through to let Bash.checkPermissions auto-allow; decisionReason 'sandboxOverride' has sub-tags excludedCommand|dangerouslyDisableSandbox for run-outside-sandbox prompts. The SDK exposes a `sandbox` settings object.","ccSource":"spec 09 §4.5,§6.1 step1b; src/utils/permissions/permissions.ts","verdict":"configurable","sdkSurface":"sandbox (SandboxSettings) + settings permissions","bridge":"SDK `sandbox` config enables the same command-sandbox; the auto-allow-if-sandboxed and sandbox-override(dangerouslyDisableSandbox) decision branches are engine-internal Bash-tool logic. Client controls whether sandboxing is on, not the per-decision override semantics.","targetPhase":"2","confidence":"inferred","snapshot":"feb"},
  {"id":"09.15","area":"09-permission-system","feature":"PermissionRequest hook overriding the verdict","what":"On an 'ask', PreToolUse PermissionRequest hooks run (async-iterable, first allow/deny wins); in headless they run before auto-deny, in coordinators sequentially before the dialog. Hook output: { behavior:'allow'|'deny', updatedInput?, updatedPermissions?, message?, interrupt? }; interrupt aborts the turn. The SDK has PermissionRequest as a first-class hook event.","ccSource":"spec 09 §6.14; src/types/hooks.ts PermissionRequest","verdict":"provided","sdkSurface":"hooks PermissionRequest event (HookEvent) + HookPermissionDecision allow|deny|ask|defer","bridge":"SDK PermissionRequest hook returns HookPermissionDecision (adds 'defer' vs CC's allow/deny). updatedInput/updatedPermissions/interrupt carry over. The 'first hook wins' iteration and headless-before-auto-deny ordering are engine-internal but the event + output contract are exposed.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.16","area":"09-permission-system","feature":"PermissionDenied hook + permission_denied system message","what":"CC tracks denials and surfaces them; the SDK emits a PermissionDenied hook event and an SDKPermissionDeniedMessage (system/permission_denied) when a request is denied.","ccSource":"spec 09 §6.12; src/entrypoints/sdk/coreSchemas.ts permission_denials","verdict":"provided","sdkSurface":"hooks PermissionDenied event + SDKPermissionDeniedMessage (system/permission_denied)","bridge":"SDK exposes denial both as a hook event (PermissionDenied) and as a streamed system message — richer than CC's internal denial counter. Direct parity on observing denials.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.17","area":"09-permission-system","feature":"SDK denial envelope in terminal result (permission_denials[])","what":"QueryEngine.wrappedCanUseTool pushes every non-allow result onto permissionDenials, serialized into the terminal result message as permission_denials: [{tool_name, tool_use_id, tool_input}]. The SDK's SDKResultMessage carries the equivalent denial array.","ccSource":"spec 09 §6.12; src/entrypoints/sdk/coreSchemas.ts:1399-1445","verdict":"provided","sdkSurface":"SDKResultMessage (permission_denials) + SDKPermissionDeniedMessage","bridge":"This is literally the SDK contract (coreSchemas is the SDK entrypoint). Direct parity: clients read permission_denials off the result envelope.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.18","area":"09-permission-system","feature":"MCP permission-prompt tool routing (permissionPromptToolName)","what":"CC can route ask decisions through an MCP permission-prompt tool: inputSchema {tool_name,input,tool_use_id}; output allow{updatedInput,updatedPermissions,decisionClassification} | deny{message,interrupt}. permissionPromptToolResultToPermissionDecision applies updatedPermissions and on interrupt aborts. The SDK exposes `permissionPromptToolName`.","ccSource":"spec 09 §6.13; src/utils/permissions/PermissionPromptToolResultSchema.ts:15-127","verdict":"provided","sdkSurface":"permissionPromptToolName (Options) + canUseTool","bridge":"SDK `permissionPromptToolName` routes prompts through a named MCP tool with the same allow/deny + updatedInput/updatedPermissions/interrupt contract. decisionClassification (user_temporary|user_permanent|user_reject) carries through. Empty updatedInput = 'use original' edge case is engine-handled.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.19","area":"09-permission-system","feature":"Auto-mode YOLO classifier (TRANSCRIPT_CLASSIFIER, XML 2-stage / tool_use)","what":"In 'auto' mode CC runs a side-channel LLM classifier (classify_result tool or XML stage1/stage2) returning {shouldBlock,reason}; fast-paths via SAFE_YOLO_ALLOWLISTED_TOOLS and acceptEdits-would-allow; iron-gate fail-closed on classifier unavailability. The SDK exposes permissionMode:'auto' but not the classifier internals.","ccSource":"spec 09 §5.6-5.7,§6.4-6.9; src/utils/permissions/yoloClassifier.ts","verdict":"configurable","sdkSurface":"permissionMode:'auto' + filterEscalatingDefaultMode","bridge":"Selecting `auto` activates the engine's classifier; the prompt build, XML stages, allowlist, iron-gate, and ANT-only env tuning (CLAUDE_CODE_TWO_STAGE_CLASSIFIER, AUTO_MODE_MODEL) are all server/engine-side and NOT client-configurable. autoMode allow/soft_deny/environment descriptions come from settings (autoMode), partly bridgeable via settings.","targetPhase":"2","confidence":"doc","snapshot":"feb"},
  {"id":"09.20","area":"09-permission-system","feature":"Bash prompt-rule classifier (BASH_CLASSIFIER, Bash(prompt: ...))","what":"ANT-only: per-command classifier exposed via `Bash(prompt: <description>)` rules, raced speculatively (≤2s) against user input in the interactive handler. Lets users auto-approve described command classes. External builds lack it.","ccSource":"spec 09 §3,§6.2,§11; src/utils/permissions/bashClassifier.ts (external stub)","verdict":"not-possible","sdkSurface":"","bridge":"`Bash(prompt: ...)` prompt-rule classification is gated behind feature('BASH_CLASSIFIER') (ANT-only); the external SDK ships the stub that always returns matches:false. No SDK surface enables prompt-based Bash rules; only literal/wildcard Bash rules work externally.","targetPhase":"3","confidence":"doc","snapshot":"feb"},
  {"id":"09.21","area":"09-permission-system","feature":"Denial tracking + fallback-to-prompting limits (3 consecutive / 20 total)","what":"DenialTrackingState {consecutiveDenials,totalDenials} with DENIAL_LIMITS maxConsecutive=3, maxTotal=20; shouldFallbackToPrompting at the limits; auto-mode resets consecutive on allow. localDenialTracking overrides appState for headless subagents. No SDK knob for these thresholds.","ccSource":"spec 09 §5.5; src/utils/permissions/denialTracking.ts:7-45","verdict":"build","sdkSurface":"","bridge":"The denial-limit thresholds and fallback-to-prompting behavior are internal to the auto-mode/headless paths; not surfaced or configurable from the SDK. A client wanting equivalent backoff would track denials itself via the result envelope.","targetPhase":"3","confidence":"inferred","snapshot":"feb"},
  {"id":"09.22","area":"09-permission-system","feature":"dontAsk mode (ask → deny if not pre-approved)","what":"In dontAsk mode, any 'ask' verdict becomes a deny with DONT_ASK_REJECT_MESSAGE; only pre-approved (rule-allowed) tools run. The SDK PermissionMode includes 'dontAsk' with the same semantics.","ccSource":"spec 09 §6.1; src/utils/permissions/permissions.ts:756-759","verdict":"provided","sdkSurface":"permissionMode:'dontAsk'","bridge":"Direct 1:1 — dontAsk converts ask→deny in both. SDK clients combine it with allow rules / canUseTool pre-approval to permit specific tools without prompting.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.23","area":"09-permission-system","feature":"acceptEdits mode (auto-accept file-edit operations)","what":"acceptEdits auto-accepts edit-class tools; auto-mode's fast-path A also checks 'would acceptEdits allow this?'. Relies on tool isReadOnly/edit classification. The SDK PermissionMode 'acceptEdits' provides the same auto-accept.","ccSource":"spec 09 §4.1; src/utils/permissions/permissions.ts auto fast-path A","verdict":"provided","sdkSurface":"permissionMode:'acceptEdits'","bridge":"Direct parity. The edit-vs-non-edit classification driving acceptEdits is engine-internal (CC's tool isReadOnly/isDestructive); SDK clients can't re-classify which tools count as 'edits'.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.24","area":"09-permission-system","feature":"plan mode (no tool execution) + prePlanMode restore + planModeInstructions","what":"plan mode designs without executing; prePlanMode records the prior mode to restore on exit; transitionPermissionMode handles plan attachments. The SDK has permissionMode:'plan' plus a `planModeInstructions` Option to customize the plan workflow body.","ccSource":"spec 09 §4.6,§6.16; src/utils/permissions/permissionSetup.ts","verdict":"provided","sdkSurface":"permissionMode:'plan' + planModeInstructions (Option) + Query.setPermissionMode","bridge":"SDK adds `planModeInstructions` (custom plan body) beyond CC's plain mode toggle. prePlanMode restore-on-exit is engine-internal; SDK clients drive entry/exit via setPermissionMode.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.25","area":"09-permission-system","feature":"Mode cycling (Shift+Tab order) + auto-mode dangerous-rule strip/restore","what":"getNextPermissionMode defines the Shift+Tab cycle (default→acceptEdits→plan→bypass/auto→default, ANT-variant differs); entering auto strips dangerous allow rules (strippedDangerousRules) restored on exit. This is interactive-TUI behavior.","ccSource":"spec 09 §4.6,§6.16; src/utils/permissions/getNextPermissionMode.ts","verdict":"configurable","sdkSurface":"Query.setPermissionMode + permissionMode","bridge":"SDK clients set mode directly via setPermissionMode rather than cycling; the Shift+Tab order is a TUI affordance. The auto-mode dangerous-rule strip/restore (filterEscalatingDefaultMode helps escalation filtering) is partly mirrored by `filterEscalatingDefaultMode`, but the strip-on-enter/restore-on-exit lifecycle is engine-internal.","targetPhase":"2","confidence":"inferred","snapshot":"feb"},
  {"id":"09.26","area":"09-permission-system","feature":"Interactive multi-racer permission resolution (dialog/bridge/channel/hooks/classifier/abort)","what":"useCanUseTool races up to 6 concurrent resolvers (user dialog, CCR bridge, channel relay, PermissionRequest hooks, Bash classifier, abort) through one createResolveOnce/claim() guard; first wins. This is the interactive front-end of the ask path.","ccSource":"spec 09 §6.2,§6.3,§8; src/hooks/toolPermission/handlers/interactiveHandler.ts","verdict":"build","sdkSurface":"","bridge":"The race orchestration (bridge/channel/classifier/dialog) is TUI+integration internal. For SDK clients the equivalent is: canUseTool callback resolves the decision (single resolver). Bridge/channel relay (CCR, Telegram) and the speculative-classifier race are not SDK-exposed.","targetPhase":"3","confidence":"inferred","snapshot":"feb"},
  {"id":"09.27","area":"09-permission-system","feature":"additionalWorkingDirectories permission (workingDir decisionReason)","what":"CC tracks additionalWorkingDirectories (Map of allowed dirs by source); file ops outside cwd produce a 'workingDir' ask/deny unless the dir is added. add/removeDirectories PermissionUpdates manage the set. The SDK exposes `additionalDirectories` Option and addDirectories/removeDirectories updates.","ccSource":"spec 09 §4.5,§4.6,§5.4; src/types/permissions.ts AdditionalWorkingDirectory","verdict":"provided","sdkSurface":"additionalDirectories (Option) + cwd + PermissionUpdate add/removeDirectories + suggestions","bridge":"SDK `additionalDirectories` seeds the allowed-dir set; runtime add/remove via PermissionUpdate (in updatedPermissions / canUseTool options carry blockedPath). The workingDir-check decision branch is engine-internal but the dir-list inputs are fully exposed.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.28","area":"09-permission-system","feature":"canUseTool callback options surface (suggestions, blockedPath, decisionReason, title, agentID)","what":"CC's PermissionContext threads rich per-decision metadata. The SDK's CanUseTool callback receives options carrying suggestions: PermissionUpdate[], blockedPath, decisionReason, title, displayName, description, toolUseID, agentID — the engine's pre-computed context handed to the client.","ccSource":"spec 09 §4.7,§6.2; src/hooks/useCanUseTool.tsx:27","verdict":"provided","sdkSurface":"canUseTool options {signal, suggestions, blockedPath, decisionReason, title, displayName, description, toolUseID, agentID}","bridge":"The SDK actually exposes MORE structured per-call metadata to canUseTool than CC documents passing to a single hook — suggestions (proposed PermissionUpdates the user can accept) and decisionReason let a client reproduce CC's dialog logic in code.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.29","area":"09-permission-system","feature":"decisionClassification on permission results (user_temporary/user_permanent/user_reject)","what":"MCP permission-prompt and SDK PermissionResult carry decisionClassification ∈ user_temporary|user_permanent|user_reject to record whether an approval was one-shot or persisted. The SDK PermissionResult allow/deny both accept decisionClassification.","ccSource":"spec 09 §6.13; PermissionPromptToolResultSchema.ts decisionClassification","verdict":"provided","sdkSurface":"PermissionResult.decisionClassification (allow & deny)","bridge":"Direct parity. Clients set decisionClassification on the canUseTool result to signal persistence intent; pairs with updatedPermissions for permanent rules.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.30","area":"09-permission-system","feature":"deny.interrupt → abort the turn","what":"A deny with interrupt:true calls toolUseContext.abortController.abort(), halting the conversation (used by MCP perm-prompt deny and PermissionRequest hooks). The SDK PermissionResult deny carries `interrupt?` with the same effect.","ccSource":"spec 09 §6.13,§6.14; src/types/permissions.ts deny + abortController","verdict":"provided","sdkSurface":"PermissionResult deny.interrupt + canUseTool","bridge":"Direct 1:1 — `behavior:'deny', interrupt:true` from canUseTool aborts the run, matching CC's abort-on-interrupt-deny.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.31","area":"09-permission-system","feature":"bypassPermissions killswitch (Statsig-gated kickout)","what":"bypassPermissionsKillswitch can revoke bypass+auto modes via Statsig (server kicks a session out of bypass; EXIT_REASON 'bypass_permissions_disabled'). The SDK surfaces the exit reason but not the killswitch config.","ccSource":"spec 09 §0; src/utils/permissions/bypassPermissionsKillswitch.ts","verdict":"not-possible","sdkSurface":"","bridge":"The Statsig-driven kickout is server-policy; the SDK observes its effect via EXIT_REASONS 'bypass_permissions_disabled' but cannot configure or disable the killswitch. allowDangerouslySkipPermissions is the only client-side gate.","targetPhase":"3","confidence":"inferred","snapshot":"feb"},
  {"id":"09.32","area":"09-permission-system","feature":"managedSettings restrictive-only permission policy (policySettings/MDM)","what":"policySettings are read-only org-pushed rules users can't override (allowManagedPermissionRulesOnly mode strips all but managed rules). The SDK's `managedSettings` Option is filtered restrictive-only from the parent process.","ccSource":"spec 09 §3,§0 (→§27); src/utils/permissions/permissionsLoader.ts","verdict":"provided","sdkSurface":"managedSettings (Option, restrictive-only) + ResolvedSettingSource 'managed'","bridge":"SDK `managedSettings` mirrors CC's policySettings tier: read-only, restrictive-only, highest precedence. The MDM/disk discovery of managed settings is engine-internal; the SDK lets the parent inject them directly.","targetPhase":"1","confidence":"doc","snapshot":"feb"},
  {"id":"09.33","area":"09-permission-system","feature":"Shadowed/unreachable rule detection","what":"shadowedRuleDetection flags allow rules made unreachable by broader deny rules (a UX/lint aid for rule editing). No SDK equivalent — rule authoring validation is client-side.","ccSource":"spec 09 §0; src/utils/permissions/shadowedRuleDetection.ts","verdict":"build","sdkSurface":"","bridge":"The SDK does not expose shadowed-rule analysis; a client building a rule editor would re-implement it using the documented matcher semantics. Pure tooling, not a runtime gate.","targetPhase":"3","confidence":"inferred","snapshot":"feb"},
  {"id":"09.34","area":"09-permission-system","feature":"requiresUserInteraction tools stay 'ask' even under bypass","what":"Step 1e: if tool.requiresUserInteraction() and result is 'ask', the ask is returned even in bypass mode (e.g. AskUserQuestion-class tools can't be auto-allowed). No SDK author knob sets requiresUserInteraction on custom tools.","ccSource":"spec 09 §6.1 step1e; src/Tool.ts requiresUserInteraction","verdict":"build","sdkSurface":"","bridge":"The requiresUserInteraction predicate is a built-in Tool field not exposed to SDK custom-tool authors; the bypass-immunity it confers is engine-internal. Clients cannot declare a custom tool as interaction-requiring.","targetPhase":"3","confidence":"inferred","snapshot":"feb"},
  {"id":"09.35","area":"09-permission-system","feature":"permission dialog UX / rule-editing screens","what":"CC's interactive permission dialogs, accept/reject checkmark display, rule-editing screens and the auto-mode ribbon (~49 reference components). This is the permission UI, not the engine.","ccSource":"spec 09 §6.2,§6.3 (→§37); src/components/permissions/*","verdict":"build","sdkSurface":"","bridge":"permission dialog UX → see C11 (37a). The engine emits ask/deny/permission_denied events and exposes canUseTool; rendering a prompt UI is the SDK consumer's responsibility (headless SDK has no dialog).","targetPhase":"3","confidence":"inferred","snapshot":"feb"}
]
