# Since-February delta

Capabilities present in current Claude Code / the Agent SDK but **absent from the February source snapshot**. Count: **30**. These were caught by the reconciliation pass (diffing the current surface against the Feb-derived rows), not incidentally.

| id | feature | verdict | SDK surface | bridge / gap | phase | conf | snap |
|---|---|---|---|---|---|---|---|
| 10.5 | Assistant-mode auto-background budget (KAIROS) | ✅ provided | Native BashOutput.assistantAutoBackgrounded (sdk-tools.d.ts) — the assistant-mode auto-background signal. | Auto-background budget surfaced by the native Bash tool output. | P3 | doc | post-feb |
| 11.17 | Turn-end file persistence (BYOC/cloud) | ✅ provided | Native SDKFilesPersistedEvent (subtype 'files_persisted', sdk.d.ts) — turn-end file persistence event. | Turn-end file persistence is a native SDK event. | Pnon-goal | doc | post-feb |
| 12.9 | Grep -o only-matching flag | ✅ provided | GrepInput['-o'] (sdk-tools.d.ts:609) | SDK's bundled Grep adds `-o`, which is ABSENT from the Feb spec input schema — the live SDK tool is a superset of the Feb CC tool here. | P1 | doc | post-feb |
| 12.13 | Embedded search-tool bundle (ANT bfs/ugrep) | ✅ provided | Native Glob + Grep tools (rg-backed) in the default query() tool list. | Embedded search is provided by native Glob/Grep; bundling bfs/ugrep is unnecessary. | Pnon-goal | doc | post-feb |
| 14.20 | Fork-yourself subagent (implicit fork) | ✅ provided | Native Agent subagent_type:'fork' — 'forks always inherit the parent model' (sdk-tools.d.ts:423). | The fork-self primitive is native on the Agent tool. | P2 | doc | post-feb |
| 15.1 | Durable task list (TaskCreate) | ✅ provided | Native TaskCreate tool — TaskCreateInput (subject/description/activeForm/metadata) at sdk-tools.d.ts:2317; present in a bare query() init tool list (runtime-verified 2026-06-16). | The model already has native TaskCreate by default. The native store is session-scoped (~/.claude/tasks/<session_id>/), NOT shared across query() sessions, so a multi-session swarm still needs a shared store — supplied by the harness file-backed TaskStore (cluster 30; see CORRECTIONS-2026-06-16-native-tools.md). | P1 | verified | post-feb |
| 15.2 | Task update / status workflow (TaskUpdate) | ✅ provided | Native TaskUpdate tool — TaskUpdateInput with status (incl. deleted), addBlocks/addBlockedBy (DAG), owner at sdk-tools.d.ts:2343; in the default query() tool list (runtime-verified 2026-06-16). | Native TaskUpdate covers the status workflow + dependencies + owner. TaskCreated/TaskCompleted hook events also exist. Shared-across-peers claim semantics still need the harness store (cluster 30). | P1 | verified | post-feb |
| 15.3 | Task retrieval and listing (TaskGet / TaskList) | ✅ provided | Native TaskGet (TaskGetInput.taskId, sdk-tools.d.ts:2337) + TaskList (TaskListInput {}, 2383); both in the default query() tool list (runtime-verified 2026-06-16). | Native TaskGet/TaskList provide read-only retrieval. The harness store mirrors this for the cross-session swarm list (cluster 30). | P1 | verified | post-feb |
| 15.4 | Session todo checklist (TodoWrite V1) | ✅ provided | Native TodoWrite tool — TodoWriteInput at sdk-tools.d.ts:678; present in the default query() tool list (runtime-verified 2026-06-16). | Native TodoWrite is the session checklist. CC disables V1 TodoWrite by default in favor of Task* V2, so prefer the Task tools. | P1 | verified | post-feb |
| 15.9 | Task dependencies (blocks / blockedBy) | ✅ provided | Native TaskUpdate.addBlocks/addBlockedBy (sdk-tools.d.ts:2343) build the blocks/blockedBy DAG; the native task file carries blocks/blockedBy arrays (runtime-verified 2026-06-16). | The native task store already models the blocks/blockedBy DAG. Cross-session claim/blocking for the swarm uses the harness store's CAS (cluster 30). | P1 | verified | post-feb |
| 15.10 | Task ownership / claim by agent | ✅ provided | Native TaskUpdate.owner (sdk-tools.d.ts:2372) + TaskListOutput.owner (sdk-tools.d.ts:3185). | Task ownership/claim is a native field; assignee notification rides the native peer mailbox (30.3). Auto-claim-on-in_progress is the only behavior atop the native data model. | P2 | doc | post-feb |
| 15.12 | Verification-agent nudge on task completion | ✅ provided | Native TaskCompleted hook (TaskCompletedHookInput {task_id,task_subject,teammate_name}) + agentic verifier hook type:'agent' (sdk.d.ts:4813). | Both the completion trigger and the verifier-agent spawn are native SDK hook surfaces. | P3 | doc | post-feb |
| 19.90 | ScheduleWakeup tool (/loop dynamic self-pacing) | ✅ provided | Native ScheduleWakeup tool — ScheduleWakeupInput (delaySeconds clamped [60,3600], reason, /loop prompt, sdk-tools.d.ts:2455); in the default query() tool list (runtime-verified 2026-06-16). | ScheduleWakeup for /loop dynamic self-pacing is a native tool. | P2 | verified | post-feb |
| 19.91 | DesignSync tool | 🏗 build | — | Not exposed by the SDK. Would be a custom tool (createSdkMcpServer) or harness feature. Confirm exact behavior against current CC before building. | P3 | inferred | post-feb |
| 21a.7 | /goal — keep working toward a completion condition (post-Feb) | 🔧 configurable | Options.hooks Stop/StopFailure callback + agents/initialPrompt; AgentDefinition.maxTurns; a custom Stop hook re-prompts until the goal evaluator passes | Reproduce with a Stop hook that evaluates the goal condition (fast model) and re-injects a continue prompt until satisfied; the SDK Stop/UserPromptSubmit hooks expose the needed loop control. Bundled CLI /goal is post-Feb and not in supportedCommands() for the Feb snapshot. | P2 | inferred | post-feb |
| 21d.4 | Bundled /deep-research workflow command (post-Feb) | 🔧 configurable | Reproducible with Options.agents (Explore-style research subagents, background:true), WebSearch/WebFetch tools, and agentProgressSummaries; bundled command runs in current CLI via supportedCommands() | In current CC it ships as a bundled command (enumerable via supportedCommands()); for the Feb snapshot, reproduce with Options.agents + WebSearch/WebFetch orchestration. Not present in the Feb catalog. | P3 | inferred | post-feb |
| 28.6 | Plugin marketplace install (npm/github/url/git/pip sources) | 🔧 configurable | Native Settings.extraKnownMarketplaces + enabledPlugins + SDKPluginInstall message (sdk.d.ts). | Plugin marketplace install is wired via native settings + plugin-install surface; we supply the source-spec parsing. | P3 | doc | post-feb |
| 30.1 | Multi-agent swarm spawn (teammates) | 🔧 configurable | Native Agent tool: name ('addressable via SendMessage'), mode (incl 'plan'/'bubble'), isolation 'worktree'\|'remote' (sdk-tools.d.ts:431-445) + Options.teammateMode 'auto'\|'tmux'\|'in-process' (sdk.d.ts:6062). Experimental, gated by CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS (assistant.mjs). Live-confirmed headless: model spawned Agent(name=helper)+SendMessage with the flag (2026-06-16). | Native teammate spawn exists but is experimental/flag-gated/off-by-default and did NOT demonstrably share task state; the harness keeps a controlled non-experimental swarm. See CORRECTIONS-2026-06-16-native-tools.md. | P2 | verified | post-feb |
| 30.2 | Create / disband an agent team (TeamCreate / TeamDelete) | 🔧 configurable | Native single implicit team — Agent.team_name 'Deprecated; ignored. The session has a single implicit team' (sdk-tools.d.ts:435). | Team create/disband is implicit natively (experimental). Harness exposes explicit TeamCreate/Delete over its non-experimental runtime. | P2 | doc | post-feb |
| 30.3 | Inter-agent messaging (SendMessage) | 🔧 configurable | Native SendMessage tool (in init tools when CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS set; live-confirmed 2026-06-16) + SDKMessageOrigin kind:'peer' (sdk.d.ts:3703); isolatePeerMachines gates cross-machine SendMessage (sdk.d.ts:6070). | Native SendMessage + peer origins provide inter-agent messaging (experimental/flag-gated). Harness mailbox is the non-experimental equivalent. | P2 | verified | post-feb |
| 30.4 | Coordinator orchestrator mode | 🔧 configurable | Native SDKMessageOrigin kind:'coordinator' (sdk.d.ts:3714) + AgentDefinition.tools/disallowedTools for the coordinator tool pool. | Coordinator is a native message origin in the experimental teammate system; harness supplies the non-experimental persona + whitelist. | P2 | doc | post-feb |
| 30.5 | Worker->leader permission bridge | 🔧 configurable | Native per-teammate permission via Agent.mode ('plan'\|'bubble'\|'acceptEdits'\|'dontAsk'\|...) (sdk-tools.d.ts:439-441) + canUseTool (sdk.d.ts:1351). | Per-teammate permission mode is native (experimental). Harness PermissionBroker centralizes canUseTool for its non-experimental peers (A2b). | P2 | doc | post-feb |
| 30.7 | Structured teammate protocol (shutdown / plan-approval) | 🔧 configurable | Native plan-approval — ExitPlanModeOutput.awaitingLeaderApproval + requestId (sdk-tools.d.ts:2663,2667); graceful shutdown — SDKWorkerShuttingDownMessage subtype 'worker_shutting_down' (sdk.d.ts:4261). | Both handshakes exist natively in the experimental teammate system. Harness reproduces them (A2b) for its non-experimental swarm. | P2 | verified | post-feb |
| 31.2 | Periodic synthetic <tick> prompt injection (self-wake) | ✅ provided | Native ScheduleWakeup tool (delaySeconds, reason, /loop prompt + '<<autonomous-loop-dynamic>>' sentinel, sdk-tools.d.ts:2455) + SDKUserMessage priority/isSynthetic/shouldQuery via streamInput (sdk.d.ts:4184). | Self-wake / synthetic tick is the native ScheduleWakeup + synthetic-user-message surface. | P2 | verified | post-feb |
| 31.3 | SleepTool — model-controlled inter-action pacing / wake-on-queue | ✅ provided | Native ScheduleWakeup (model-controlled pacing) + Monitor tool (wake-on-event, stop via TaskStop), both in the default query() tool list (runtime-verified 2026-06-16). | SleepTool pacing maps onto native ScheduleWakeup/Monitor. | P2 | verified | post-feb |
| 31.7 | /goal — keep working until a condition is met (post-Feb) | 🏗 build | — | Rebuild as a Stop-hook (HOOK_EVENTS 'Stop') that re-prompts unless a fast model judges the goal met, plus a maxTurns/maxBudgetUsd guard. SDK exposes the Stop hook and a model-swap path, but the goal-evaluator loop and the /goal command are yours to assemble. | P2 | inferred | post-feb |
| 32.4 | Channels: bidirectional MCP-over-channel inbound + permission relay | 🔧 configurable | Native channelsEnabled + allowedChannelPlugins settings + SDKControlChannelEnableRequest + SDKMessageOrigin kind:'channel' (sdk.d.ts:3700); permission relay via Elicitation hooks + canUseTool. | Inbound channel transport + permission relay are native (settings-gated); only the concrete adapter MCP servers are external. | P2 | doc | post-feb |
| 32.5 | Cron / scheduled triggers (CronCreate/Delete/List + /loop) | ✅ provided | Native CronCreate/CronDelete/CronList tools (5-field cron, prompt, recurring, durable→.claude/scheduled_tasks.json, 7-day expiry; sdk-tools.d.ts:2430) + SessionCronSummary (sdk.d.ts). | Scheduler, cron store, and one-shot-vs-recurring logic are native. | P2 | verified | post-feb |
| 32.8 | Push notifications (PushNotificationTool) + phone push | ✅ provided | Native PushNotification tool (status:'proactive'; output pushSent/localSent/disabledReason/hasFocus/idleSec, sdk-tools.d.ts:2519) + agentPushNotifEnabled/inputNeededNotifEnabled settings. | Both desktop and phone push legs are native (pushSent = mobile). | P2 | doc | post-feb |
| 42.90 | "ultracode" workflow opt-in keyword | 🏗 build | — | Harness-level UX convention layered on the (Feb-present) Workflow engine. Implement as a prompt/keyword trigger in your harness loop; no SDK surface. | P2 | inferred | post-feb |
