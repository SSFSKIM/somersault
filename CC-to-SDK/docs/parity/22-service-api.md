# Parity — 22-service-api

| id | feature | verdict | SDK surface | bridge / gap | phase | conf | snap |
|---|---|---|---|---|---|---|---|
| 22.1 | First-party Anthropic API transport (api.anthropic.com) | ✅ provided | Options.model/fallbackModel; ANTHROPIC_API_KEY via env/SDK; bundled CLI is the same engine; supportedModels()/accountInfo() | Provide an API key (ANTHROPIC_API_KEY) and the SDK's bundled CLI runs the identical first-party messages.create pipeline. | P1 | doc | feb |
| 22.2 | AWS Bedrock provider transport | ✅ provided | env: CLAUDE_CODE_USE_BEDROCK (+ AWS_REGION/AWS_BEARER_TOKEN_BEDROCK); passed through Options.env to the bundled CLI | Set CLAUDE_CODE_USE_BEDROCK=1 plus AWS credentials in the SDK's `env` (spread process.env first); the CLI selects the Bedrock client. | P1 | doc | feb |
| 22.3 | Google Vertex AI provider transport | ✅ provided | env: CLAUDE_CODE_USE_VERTEX (+ GCLOUD_PROJECT/CLOUD_ML_REGION/GOOGLE_APPLICATION_CREDENTIALS) via Options.env | Set CLAUDE_CODE_USE_VERTEX=1 and GCP project/region in the SDK `env`; the CLI selects the Vertex client. | P1 | doc | feb |
| 22.4 | Azure AI Foundry provider transport | ✅ provided | env: CLAUDE_CODE_USE_FOUNDRY (+ ANTHROPIC_FOUNDRY_API_KEY/_RESOURCE/_BASE_URL) via Options.env | Set CLAUDE_CODE_USE_FOUNDRY=1 plus Foundry resource/key in the SDK `env`; the CLI selects the Foundry client. | P1 | doc | feb |
| 22.5 | Fork-specific OpenAI provider path (src/services/api/openai.ts) | 🏗 build | — | The Agent SDK is the Anthropic/CLI engine only; an OpenAI provider must be built outside the SDK (custom transport or a parallel client). Not reachable via Options. | Pnon-goal | inferred | feb |
| 22.6 | Custom base URL / gateway / proxy override | 🔧 configurable | Options.env (ANTHROPIC_BASE_URL/ANTHROPIC_CUSTOM_HEADERS/proxy); Options.extraArgs for raw CLI flags | Set ANTHROPIC_BASE_URL and custom headers through the SDK `env`; the bundled CLI forwards them to the Anthropic client. | P1 | doc | feb |
| 22.7 | Model registry / capability matrix (thinking, fast-mode, structured outputs, 1M context) | ✅ provided | Options.model/fallbackModel/thinking/effort/betas(['context-1m-2025-08-07'])/outputFormat; Query.supportedModels() | The bundled CLI ships the same model registry; set model/thinking/effort/betas/outputFormat and capabilities are applied identically. Query.supportedModels() reads the registry. | P1 | doc | feb |
| 22.8 | Streaming SSE event loop (message_start → content_block_* → message_stop) | ✅ provided | query() async-iterates SDKMessage; Options.includePartialMessages → SDKPartialAssistantMessage (stream_event) | The CLI runs the same SSE loop; enable includePartialMessages to receive streaming partials, otherwise consume assistant/result messages. | P1 | doc | feb |
| 22.9 | Retry with exponential backoff + 529 cascade and retry-after handling | ✅ provided | internal to bundled CLI; SDKAPIRetryMessage (system/api_retry) surfaces retries; CLAUDE_CODE_MAX_RETRIES via env | Retry/backoff is handled inside the CLI engine; observe SDKAPIRetryMessage and tune CLAUDE_CODE_MAX_RETRIES via Options.env. No separate SDK API to override the algorithm. | P1 | doc | feb |
| 22.10 | Streaming → non-streaming fallback on stream errors / 404 | ✅ provided | internal to bundled CLI; CLAUDE_CODE_DISABLE_NONSTREAMING_FALLBACK env to disable | Fallback is automatic in the CLI engine; disable via CLAUDE_CODE_DISABLE_NONSTREAMING_FALLBACK in Options.env if needed. | P1 | inferred | feb |
| 22.11 | Normalized API error taxonomy surfaced to the agent | ✅ provided | SDKResultMessage error subtypes; SDKAssistantMessageError ('rate_limit'\|'overloaded'\|'authentication_failed'\|'oauth_org_not_allowed'\|'billing_error'\|...) | The CLI emits the same error classification via SDKResultMessage subtypes and SDKAssistantMessageError; read those discriminants. | P1 | doc | feb |
| 22.12 | Prompt-cache breakpoint placement and 1h TTL eligibility | ✅ provided | internal to bundled CLI (no SDK Option toggles individual breakpoints); DISABLE_PROMPT_CACHING env | Prompt caching is managed by the CLI engine; control coarsely via DISABLE_PROMPT_CACHING* env in Options.env. Breakpoint placement is not exposed. | P2 | inferred | feb |
| 22.13 | Token-usage accumulation for cost tracking | ✅ provided | SDKResultMessage.usage; Query.getContextUsage(); usage_EXPERIMENTAL_… ; SDKThinkingTokensMessage | Usage totals arrive on SDKResultMessage and via Query.getContextUsage(); the same accumulation runs in the CLI. | P1 | doc | feb |
| 22.14 | Files API upload/download (session attachments) | 🏗 build | — | No SDK Option exposes the Files API; file attachment plumbing would be built on the Anthropic SDK directly or via custom tooling. Not part of the agent loop surface. | P3 | inferred | feb |
| 22.15 | Bootstrap fetch (/api/claude_cli/bootstrap client/model config) | ✅ provided | internal to bundled CLI startup (no SDK Option); runs automatically with first-party auth | Bootstrap runs inside the CLI engine when using first-party auth; nothing to wire. (Skipped/irrelevant for Bedrock/Vertex/Foundry.) | P1 | inferred | feb |
| 22.16 | Task / budget params (taskBudget, max budget USD) | ✅ provided | Options.taskBudget ({total}); Options.maxBudgetUsd → SDKResultMessage subtype error_max_budget_usd | Set taskBudget and/or maxBudgetUsd in Options; the CLI assembles the same output_config and emits the budget-error result subtype. | P1 | doc | feb |
| 22.17 | claude.ai (Max/Pro subscription) inference rate limits | 🚫 not-possible | — | claude.ai subscription rate-limit behavior is tied to claude.ai OAuth login, which Anthropic forbids 3rd-party SDK apps from offering. Use API-key/Bedrock/Vertex/Foundry billing instead; SDKRateLimitEvent exposes API-side limits only. | Pnon-goal | inferred | feb |
| 22.18 | Generic API rate-limit event surfacing | ✅ provided | SDKRateLimitEvent (rate_limit_event) | The CLI emits SDKRateLimitEvent when rate-limit state changes; consume it from the query stream. | P1 | doc | feb |
