# The drift ritual (W4.3)

The SDK moved 33 releases in one month; `coverage.md`/`full-potential.md` rot in weeks. This is the
monthly (or on-demand: before any wave, after any SDK bump) remeasure procedure. It is deliberately a
*ritual*, not CI — steps 2–4 need judgment.

## 1. Name-level drift scan (mechanical)

```bash
node scripts/drift-check.mjs          # human report; --json for automation
```

Diffs installed vs npm HEAD `sdk.d.ts` on four surfaces: Options fields, Query methods, SDKMessage
union members, top-level exported names. Exits 2 on parse failure (false-clean guard). No drift ≠ no
change — bodies/jsdoc/semantics move without renames; skim the npm changelog when versions differ.

The same command also runs three **local gates** that exit 1 rather than merely reporting, because
unlike the HEAD comparison they are deterministic and have a right answer: the appserver scorecard's
row presence, row staleness and registry coverage, and — added M6 — the **settings-key advisory**,
`configDomain.ts`'s `KNOWN_TOP_LEVEL` against the installed SDK's generated
`export declare interface Settings`, in both directions. That last one exists because the list was
hand-transcribed and nothing was watching it: by 0.3.237 it warned about 71 real upstream keys and
blessed 9 upstream had dropped, and no test or gate could go red, since a wrong advisory only ever
costs a missing or spurious warning. On failure the gate prints the corrected body ready to paste, so
adopting a schema move is a paste rather than another hand re-read of ~150 names. It compares against
the **installed** SDK, never npm HEAD — the advisory must describe the SDK we ship against, and gating
on HEAD would turn someone else's release into our red build.

## 2. On drift: classify each name

- **Added** → a new premise. File it in `full-potential.md` §1 as 🔬 unverified; probe before
  building (the A1 lesson — declared ≠ reachable). Knob-shaped additions go through the W4.1 pattern
  (one-line resolveOptions wire + knobs.test.ts row) only *after* a probe or a structural-passthrough
  judgment.
- **Removed** → check `coverage.md` + `harness/src` consumption (`grep -rn <name> harness/src`).
  Consumed-and-removed = a break: bump + fix before the routine bump lands it on you. Also re-check
  the standing-exclusions floor — 0.3.211 *deleted* `runAssistantWorker`/`connectRemoteControl`; the
  floor shrinks on its own.

## 3. Bump + re-verify (when adopting the new version)

Bump all four package.jsons (`harness/`, `app-server/`, `probes/`, `reforge/` — the former `tui/`
package retired with A2b), then:

```bash
cd probes && set -a; . ../.env; set +a; npx tsx probes/00-health-check.ts   # ALWAYS first
cd harness && npm run typecheck && npm run test:unit                        # 560+ green
# keyed live sweep (each ~5-30s): knobs, structured, otel, session-store, mcp-topology,
# warm-pool, tenant — plus any probe touching a drifted surface
```

`test/unit/index.test.ts` (surface pin) and `knobs.test.ts` (Options mapping) are the tripwires most
likely to catch silent SDK shape changes.

## 4. Update the maps

`coverage.md` §7 remeasure note + affected domain rows; `full-potential.md` §1 rows (verdict flips),
§2 recount, standing-exclusions floor. Refresh the relevant memory file.

## Run log

| date | installed → HEAD | verdict |
| --- | --- | --- |
| 2026-07-17 | 0.3.211 → 0.3.212 | zero name-level drift on all four surfaces (63 Options fields / 32 Query methods / 39 SDKMessage members / 236 exports) — no action |
| 2026-08-18 | 0.3.227 → 0.3.234 (adopted; harness+probes+app-server bumped) | name-level: +2 exports only (`SDKContextUsage`/`SDKContextUsageCategory`). Full-text diff found what the four-surface scan can't (non-exported control types): **added** — `context_usage` structured sibling on the `/context` synthetic assistant message; init gains `effort` + `terminal_slash_commands`; `queued_notifications` capability + user-message `fromMode` (CCR-flavored); `ApiKeySource` union widened, legacy members retained. **Removed** — `get_plan` + `get_workspace_diff` control requests, `bypass_permissions_disabled` exit reason: zero consumers in `harness/src`, no premise rows, free. **Semantics** — dialog-kind contract inverted (declare in `supportedDialogKinds`, never answer undeclared kinds): our passthrough already models it. Gates: typecheck + 2891 unit + 3546 tui green; keyed health + knobs/structured/session-store + M4 acceptance. One TUI failure during verification was the worktree's 97-char cwd truncating the `/permissions` Workspace path row, not the SDK (control: same fail on 0.3.227, same cwd) — test fixed to prefix-match |
| 2026-08-21 | 0.3.234 → 0.3.237 (adopted; app-server bumped — harness and probes were already at 0.3.237 in main, app-server had been missed) | name-level: **zero drift on all four surfaces**. **The full-text `sdk.d.ts` diff again found what the scan cannot** (41 lines; the scan's clean verdict is scoped to four surfaces and this is the second consecutive bump where that scope was the difference). **Added** — `classifierContext` on a hook result (host-asserted intent for the auto-mode permission classifier; filed 🔬 in full-potential §1, unprobed, zero consumers); `account_on_hold` joins `SDKAssistantMessageError` (zero consumers, free); a `spellcheck` block joins the settings schema. **Removed** — nothing. **Doc-only** — the provider list lost `anthropicGoogleCloud` from an example and gained an "e.g.", so it is now explicitly non-exhaustive. **`spellcheck` is the live proof of the parked `KNOWN_TOP_LEVEL` rot**: it is absent from the 87 hand-transcribed keys, so `config/value/write` warns `unknown top-level settings key "spellcheck" (written anyway)` for a key upstream ships — the drift arrived on the first bump after the item was written down. Gates: health check ✅ live; typecheck clean; tui 152 files / 3872 tests; unit 235 files / 3239 tests on three consecutive runs. **One earlier unit run reported a single failure whose name was lost to output truncation and did not reproduce in three subsequent full runs** — recorded as an unidentified flake rather than called clean, because an unidentified flake is what a real regression hides behind |
| 2026-08-29 | 0.3.237 → 0.3.250 (adopted; all four package.jsons bumped — harness, app-server, probes, reforge) | name-level: **zero drift on all four surfaces** (Options 64 → 65 is the sole addition; exports, `Query` methods and the 38-member `SDKMessage` union are character-identical). **Third consecutive bump where the full-text `sdk.d.ts` diff carried the news the four-surface scan cannot see** (755 diff lines; the scan's clean verdict is scoped to four surfaces and everything below except the one Options field lives outside them). **Removed — nothing**: the only two `-` lines in the whole diff are widenings (`ManagedNotification` `subkind` gains `'projects-relay'`; `Settings.spinnerTipsOverride.tips` goes `string[]` → optional `(string \| {…})[]` and the block gains `tipsFile`/`label`). **Adopted — `perTaskStopAffordance`** (Options + the `initialize` control request), the one item in this bump that changes behaviour rather than bookkeeping: it declares that the consumer renders a per-task stop control, and when declared an interrupt on an open-input session aborts only the turn instead of killing running background agents/workflows. **Absence fails closed**, so until a caller sets it our Stop kills them. Wired as a W4.1 knob (types.ts jsdoc + one `resolveOptions` line + a `knobs.test.ts` row, plus the falsy-but-meaningful case) and NOT probed, because there is nothing runtime about it to probe: it is a declaration the CLI reads off `initialize` and a claim about OUR UI, and that claim is true — `BgTasksPanel`'s `onStop` stops a single row through `stopBgTask` → host `stop_task` → `session.stopTask` → `q.stopTask`. Left off by default: it is a per-config opt-in, first-attached-client-wins, and a library caller shipping no such control must not assert it. **Corrected two comments the bump falsified** — `reinitialize()` is no longer hook-safe mid-session (0.3.250 re-registers this query's hooks from the re-sent request, reports `hooks_applied`, and resolves any hook callback it was waiting on: a pending PreToolUse is denied with a retry notice, a pending prompt is blocked; probe 38's permission half still stands, the parked request is still deduped), and `background_tasks_changed` entries now also carry `ambient` (`task_started` additionally `is_backgrounded` + `spawn_depth`) so `bgTaskMeta.ts`'s "carries only {task_id, task_type, description}" was false. **Filed but not built** (new premises, zero consumers, unprobed): result `queued_turn_count`; the `user_message_uuid` turn-binding stamp on first-assistant / first-stream-event / error-result frames; `sdkMcpServerConfigs[server].timeout` per-server MCP tool-call timeout (with the matching `McpServerConfig.timeout`); permission-ask `default_to_no`; `ModelUsage.costBasis`; `hooks_applied` on the initialize response; and a `background_tasks_changed` snapshot now sent behind a repeated `initialize`'s success response. **Nine new top-level settings keys** (`desktopSessionCleanupPeriodDays`, `syncClaudeAiPlugins`, `modelPicker`, `modelPricing`, `managedSourcesBehavior`, `promptCacheTtl`, `subagentPromptCacheTtl`, `modelSettings`, `keybindingFlavor`) turned the M6 settings-key advisory red exactly as designed — the gate failed with the nine names and printed the corrected body, and adopting them was the intended paste, not a hand re-read; 149 → 158 keys, now an exact match. Gates: drift-check exit 0 (four surfaces clean, appserver scorecard 110 rows / 73 registered methods with no drift or staleness, advisory exact); `tsc --noEmit` clean; `vitest run test/unit` 267 files / 4149 tests green on the first run, no flake, no re-run needed. Live/keyed suite deliberately not run **RECOMMENDATION (proposal only — `scripts/drift-check.mjs` deliberately untouched): widen the scan, because three consecutive rows now say the same thing.** 0.3.234, 0.3.237 and 0.3.250 each reported clean or near-clean at name level while the full-text diff carried the real news, and this bump was the worst case yet — nine settings keys, nine new premises, one falsified in-code comment, and ONE of them (`perTaskStopAffordance`) visible to the four surfaces. That is not three incidents, it is the scan's scope being narrower than the SDK's change surface, and the gap has a cheap shape. **Two steps, and they are different kinds of thing, which is the whole point.** (1) A FIFTH GATED SURFACE — property names. The four surfaces walk only exported top-level names; every field this bump added lives one level in, on a type that is often not exported at all. Extracting the set of `name?:`/`name:` declarations from `sdk.d.ts` is the same regex-grade work the existing four already do, needs no TS parser, and would have caught `timeout`, `costBasis`, `ambient`, `is_backgrounded`, `spawn_depth`, `queued_turn_count`, `user_message_uuid`, `default_to_no` and `hooks_applied` — all nine — mechanically. It is noisier (one new interface contributes many names, so it wants qualified paths or a per-parent grouping to stay readable), which is an argument about presentation, not about whether the signal is there. (2) A FULL-TEXT ADVISORY — never a failure. The half a name diff structurally cannot see is the half that cost us most this bump: `reinitialize()`'s hook behaviour changed with NO name moving anywhere, and it falsified a probe-38 reassurance sitting in `session.ts`. Prose churns every release, so gating on it would be a red build on someone else's copy-edit; printing it is free. The design constraint worth stating up front: this diff must be taken against **the version we are LEAVING** — the previously installed pin, recoverable from git or an `npm pack` of the old version, which is exactly how this row was produced by hand — and NOT against npm HEAD, since the question at bump time is what changed between the SDK we shipped and the one we are adopting. That also keeps it consistent with the settings-key advisory's existing rule of comparing against the installed SDK rather than HEAD. Cost estimate: the fifth surface is an afternoon and slots into the existing gate; the advisory is one `npm pack` plus a `diff`, and its real cost is a human reading ~750 lines once per bump — which is the work the last three rows show is happening anyway, just without a tool to make it repeatable. |
| 2026-08-30 | 0.3.237 → 0.3.251 (adopted; harness+probes+app-server bumped together, branch `engine-core` — run in parallel with the row above's 0.3.250 adoption on main, so this row's deltas were measured from 0.3.237 and subsume that bump's; the merge kept 0.3.251 and carried `reforge` — which only the 0.3.250 round had bumped — up to it) | name-level: +1 Options field (`perTaskStopAffordance`) and +4 exports (`Pre`/`PostModelSwitchHookInput`/`SpecificOutput`) — the first name-level drift since 0.3.212, and STILL only a fraction of the story: the full-text diff (959 lines, third consecutive bump where it out-found the scan) adds **two new HOOK_EVENTS** (`PreModelSwitch`/`PostModelSwitch`, cost-governance payloads, Pre can deny/allow a switch), `SessionStart` resume-staleness fields, per-server SDK-MCP `timeout` (+ initialize `sdkMcpServerConfigs`), result `queued_turn_count` + error-result `user_message_uuid` echo, `ModelUsage.costBasis`, background-task `ambient`/`is_backgrounded`/`spawn_depth` + a reinit snapshot, `hooks_applied` (hooks re-register on repeated initialize), permission-ask `default_to_no`, origin subkind `'projects-relay'`, and 9 settings keys (`modelPicker`/`modelPricing`/`modelSettings`/`promptCacheTtl`/`subagentPromptCacheTtl`/`keybindingFlavor`/`managedSourcesBehavior`/`syncClaudeAiPlugins`/`desktopSessionCleanupPeriodDays`). **Removed — nothing** (one doc tag `CC-2656` dropped). All added premises filed 🔬 in full-potential §1; settings advisory pasted (149→158, gate green). One incidental package change: 0.3.251's `exports` map no longer exposes `./package.json`. Gates: health ✅ live; drift-check exit 0 all gates; typecheck clean; unit 4148/4148; app-server typecheck + unit green; keyed live sweep 10 files 17/17 (+1 conditional skip) incl. session/hooks/model-alias on the drifted surfaces. **TUI suite: 61 failures that were ALL environment artifacts, control-verified** — identical on 0.3.237 and in the main checkout: 60 selection/hover/popup tests assume `COLORTERM=truecolor` (green with it set), 1 imagePaste toast test flips copy when `SSH_CLIENT`/`SSH_TTY`/`SSH_CONNECTION` leak in from an SSH shell (green with them cleared) → 4727/4727 under a normalized env. The suite should pin both in its vitest env (TUI-session domain; not fixed here). **Probe wave same day (121–127):** every filed premise settled live — model-switch hooks ✅ (fire headless, deny/ask enforced), MCP timeout ✅ (exact), per-task stop 🟢 (stopTask works; interrupt spares bg tasks with OR without the declaration), task_started flags 🟡 live, queued_turn_count 🟡 present, SessionStart resume fields 🚫 dead headless, task-tools premise corrected (2.1.233; since-february 15.x rows) — verdicts in full-potential §1 |
