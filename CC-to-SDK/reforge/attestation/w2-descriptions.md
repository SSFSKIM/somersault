# Coverage attestation — W2 tool descriptions (2.1.251)

Generated by `npx tsx strangle/attest.ts` (campaign spec §3.1). The inventory is AST-derived and
complete: `strangle/branches.ts` refuses any branch-forming construct it cannot record, so a module
it cannot instrument fails the run rather than reporting a partial inventory.

- modules attested: **4** — glob-description, read-description, grep-description, webfetch-description
- branch sites: **10**, outcomes: **20**
- executed by the corpus: **14**
- reviewed exclusions: **6**
- un-adjudicated: **0**
- scenarios replayed: api-error, plain, search-tools, search-tools-lean

| branch | kind | condition | outcome | state | adjudication |
|---|---|---|---|---|---|
| `glob-description#globDescription@0:T` | if | `leanPrompt(model)` | true | executed | — |
| `glob-description#globDescription@0:F` | if | `leanPrompt(model)` | false | executed | — |
| `glob-description#globDescription@1:T` | conditional | `subagentSteer() === "default"` | true | executed | — |
| `glob-description#globDescription@1:F` | conditional | `subagentSteer() === "default"` | false | excluded | subagentSteer() is pinned to "default" by §3.3's disabled-gate environment and X6's env allowlist — its four sources are the forbidden CLAUDE_CODE_THISTLE_GREBE, empty clientData, a disabled GrowthBook flag and an unset model floor. Graded instead by description-parity.test.ts, which compares this arm against upstream byte for byte (glob steer=no_nudges). |
| `read-description#readDescription@0:T` | if | `leanPrompt(model)` | true | executed | — |
| `read-description#readDescription@0:F` | if | `leanPrompt(model)` | false | executed | — |
| `read-description#readDescription@1:T` | conditional | `pdfCapable()` | true | executed | — |
| `read-description#readDescription@1:F` | conditional | `pdfCapable()` | false | excluded | pdfCapable() is false only for a claude-3-haiku session model, which no scenario uses; a scenario could reach it but would buy one boolean for a live recording. Graded by description-parity.test.ts (read lean=true pdf=false). |
| `read-description#readDescription@2:T` | conditional | `pdfCapable()` | true | executed | — |
| `read-description#readDescription@2:F` | conditional | `pdfCapable()` | false | excluded | same session-model read on the full arm; graded by description-parity.test.ts (read lean=false pdf=false). |
| `grep-description#grepDescription@0:T` | if | `leanPrompt(model)` | true | executed | — |
| `grep-description#grepDescription@0:F` | if | `leanPrompt(model)` | false | executed | — |
| `grep-description#grepDescription@1:T` | conditional | `subagentSteer() === "default"` | true | executed | — |
| `grep-description#grepDescription@1:F` | conditional | `subagentSteer() === "default"` | false | excluded | same pinned resolver as the Glob arm above; graded by description-parity.test.ts (grep steer=counter_steer). |
| `webfetch-description#webFetchDescription@0:T` | if | `leanPrompt(model)` | true | executed | — |
| `webfetch-description#webFetchDescription@0:F` | if | `leanPrompt(model)` | false | executed | — |
| `webfetch-description#webFetchDescription@1:T` | conditional | `artifactException` | true | excluded | the artifact carve-out requires the Artifact tool to be present in the session's tool list and its two feature gates enabled; neither holds headlessly under §3.3's pinned gate state. Graded by description-parity.test.ts (webfetch lean=true artifact=true). |
| `webfetch-description#webFetchDescription@1:F` | conditional | `artifactException` | false | executed | — |
| `webfetch-description#webFetchDescription@2:T` | conditional | `artifactException` | true | excluded | same predicate on the full arm; graded by description-parity.test.ts (webfetch lean=false artifact=true). |
| `webfetch-description#webFetchDescription@2:F` | conditional | `artifactException` | false | executed | — |

## What grades an excluded branch

Every exclusion above is a branch the CORPUS does not render, not a branch nothing checks.
`strangle/description-parity.test.ts` evaluates the pinned upstream function with stubbed ports over
the full cross-product of these same branches and requires byte identity with the owned module — so
each excluded arm is graded against upstream directly rather than against a scenario's rendering of it.
