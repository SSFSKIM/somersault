// SABOTAGE LAYER (§2.5). `plain` MUST go red: SendMessage is in all 82 recorded cassettes.
export function sendMessageDescription(agentTeamContext, crossSessionEnabled) {
  return "[sabotaged send-message-description]";
}
