// SABOTAGE LAYER (§2.5). `perm-plan-mode` MUST go red: ExitPlanMode is only in the plan-mode catalog.
export function exitPlanModeDescription() {
  return "[sabotaged exit-plan-mode-description]";
}
