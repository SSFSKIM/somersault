// tui/src/theme.ts — shared terminal palette. Single source for the Claude accent so banner/spinner/
// transcript stay in lockstep. No deps (leaf module) → safe to import anywhere.
export const ACCENT = "#d97757"; // Claude brand orange (CC "claude" theme color)
