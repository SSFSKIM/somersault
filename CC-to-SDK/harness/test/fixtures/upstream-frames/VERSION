Claude Code 2.1.220 — captured 2026-07-31 — 100x40 — TERM=xterm-256color

These tracked fixtures are identity-redacted at rest. The original capture's greeting, email-derived
organization and `user@host` status segment are replaced with `▒`; no unredacted frame may be written
under this directory. The capture command rejects a tracked-fixture output path unless it receives
`--redact-masks` before the child is started or any frame is written.

From `CC-to-SDK/harness/`, create the pinned emulator environment once, then recapture with exactly:

```sh
python3 -m venv scripts/frames/.venv
scripts/frames/.venv/bin/pip install -r scripts/frames/requirements.txt
scripts/frames/.venv/bin/python3 scripts/capture-frames.py \
  --script scripts/frames/help-overlay.keys \
  --out test/fixtures/upstream-frames/help-overlay \
  --bin "claude" --cwd /tmp/frame-scratch --cols 100 --rows 40 \
  --redact-masks scripts/frames/masks.json \
  --expected-version "2.1.220 (Claude Code)"
scripts/frames/.venv/bin/python3 scripts/capture-frames.py \
  --script scripts/frames/composer-basics.keys \
  --out test/fixtures/upstream-frames/composer-basics \
  --bin "claude" --cwd /tmp/frame-scratch --cols 100 --rows 40 \
  --redact-masks scripts/frames/masks.json \
  --expected-version "2.1.220 (Claude Code)"
```

`redactions_by_frame` in `scripts/frames/masks.json` is the write-time contract. Every tracked frame
key must explicitly declare named patterns with per-pattern and total `minimum_matches`; a frame known
not to contain identity must declare an empty pattern list and `minimum_matches: 0`. Capture stages all
tracked frames and promotes them only when every required match succeeds. The patterns are narrowly
anchored to the boot dashboard; they do not redact transcript emails, UUIDs, paths, costs, durations,
percentages, token counts or timestamps. `by_frame` contains separate, dashboard-only comparison masks
for quota and status nondeterminism. Automated tests derive sanitized fake unredacted identities from the
ANSI/layout of every stored frame, prove they redact byte-for-byte to all eight fixtures, and prove a
changed ANSI boundary or unmatched required identity fails without persisting any tracked frame.
